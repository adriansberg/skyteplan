---
phase: 01-cleanup-dependencies
plan: 05
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - package.json
  - yarn.lock
  - tsconfig.json
autonomous: false
requirements:
  - DEPS-04
user_setup: []

must_haves:
  truths:
    - "typescript is at 6.0.3 on disk"
    - "yarn check passes with TypeScript 6's types:[] default"
    - "yarn build passes"
    - "Phase 1 success criteria all met: SvelteKit 2.59 + Vite 8 + TypeScript 6 stack builds cleanly"
  artifacts:
    - path: "package.json"
      provides: "typescript 6.0.3 pinned"
      contains: "typescript"
    - path: "yarn.lock"
      provides: "Resolved tree with typescript 6.0.3"
    - path: "tsconfig.json"
      provides: "Either unchanged OR with a minimal types[] override if TS6 surfaced missing-globals errors"
  key_links:
    - from: "package.json devDependencies"
      to: "node_modules/typescript/package.json"
      via: "yarn add"
      pattern: "\"typescript\": \"6\\.0\\.3\""
    - from: "tsconfig.json"
      to: ".svelte-kit/tsconfig.json (extended base provides lib: DOM)"
      via: "extends"
      pattern: "\"extends\": \"\\./\\.svelte-kit/tsconfig\\.json\""
---

<objective>
Bump TypeScript from 5→6.0.3 as the final wave. RESEARCH.md Pitfall 2 flags one risk: TS6 defaults the `types` field to `[]` instead of auto-including all `@types/*` packages. This project has no `@types/node` import in TS source and gets browser globals from `lib: [DOM, DOM.Iterable]` in the extended `.svelte-kit/tsconfig.json`, so risk is LOW. If `yarn check` does fail with "Cannot find name" errors after the bump, the fix is to add a minimal `"types": ["cookie"]` to tsconfig.json (the only `@types` package present in node_modules).

Purpose: Complete the target stack — SvelteKit 2.59, Vite 8, TypeScript 6 — and satisfy the Phase 1 goal: "App builds cleanly on the target dependency stack with all dead code removed."

Output: typescript at 6.0.3; tsconfig.json either unchanged or with a documented `types` override; final `yarn check && yarn build` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-cleanup-dependencies/01-RESEARCH.md
@.planning/phases/01-cleanup-dependencies/01-PATTERNS.md
@.planning/phases/01-cleanup-dependencies/01-04-SUMMARY.md
@package.json
@tsconfig.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify typescript@6.0.3 on npm</name>
  <what-built>About to install typescript@6.0.3. Flagged [ASSUMED] in RESEARCH.md Package Legitimacy Audit because slopcheck was unavailable.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/typescript
    2. Confirm publisher is `typescript-bot` / Microsoft organization
    3. Confirm 6.0.x line exists with 6.0.3 as a published version
    4. Confirm repository link points to github.com/microsoft/TypeScript
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the TypeScript 6 upgrade, or describe specific concerns.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bump typescript to 6.0.3 and verify tsconfig types defaults</name>
  <files>package.json, yarn.lock, tsconfig.json</files>
  <read_first>
    - package.json (current typescript ^5.0.0)
    - tsconfig.json (current compilerOptions; no `types` field present; extends `.svelte-kit/tsconfig.json`)
    - .planning/phases/01-cleanup-dependencies/01-RESEARCH.md (Installation Step 4; Pitfall 2 with detailed assessment; Assumption A3)
    - .planning/phases/01-cleanup-dependencies/01-PATTERNS.md (tsconfig.json: monitor after DEPS-04; only add `types` if check fails post-upgrade)
  </read_first>
  <action>
    Run `yarn add -D typescript@6.0.3` from the project root. Do not pre-emptively edit tsconfig.json. After yarn finishes, run `yarn check`. If `yarn check` exits 0 leave tsconfig.json unchanged — Assumption A3 from RESEARCH.md held. If `yarn check` fails specifically with "Cannot find name" errors for globals (RESEARCH.md Pitfall 2 warning sign), inspect node_modules/@types to identify the missing global source, then add a `"types"` array to tsconfig.json's compilerOptions naming only the @types package(s) actually needed (RESEARCH.md suggests starting with `"types": ["cookie"]` since that is the only @types in node_modules). Re-run `yarn check`. After `yarn check` is green, run `yarn build`. Do not bump or alter any other dependency in this task. Do not edit tsconfig.json for any reason other than the types-array fix described above; in particular do not change `strict`, `moduleResolution`, `allowJs`, or `checkJs`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const got=(p.devDependencies.typescript||'').replace(/^[\\^~]/,''); if (got !== '6.0.3') { console.error('FAIL got',p.devDependencies.typescript); process.exit(1); } console.log('OK');" && yarn check && yarn build && yarn lint</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies.typescript resolves to `6.0.3`
    - yarn.lock entry for typescript names version 6.0.3
    - `yarn check` exits 0 (with or without a tsconfig.json `types` override)
    - `yarn build` exits 0
    - `yarn lint` exits 0
    - If tsconfig.json was modified, the only change is the addition of a `"types"` field inside compilerOptions; other fields (`extends`, `allowJs`, `checkJs`, `esModuleInterop`, `forceConsistentCasingInFileNames`, `resolveJsonModule`, `skipLibCheck`, `sourceMap`, `strict`, `moduleResolution`) are unchanged
    - All Phase 1 grep gates still pass:
      - `! grep -r "svelte-query" src/ package.json`
      - `! grep -rE "formatNorwegianDateLocal|formatNorwegianTimeLocale" src/`
      - `! grep -E "beforeinstallprompt|appinstalled" src/lib/pwa.ts`
      - `test "$(grep -rl "'10782'" src/ | wc -l | tr -d ' ')" = "1"`
      - `! grep -r "2025.lsres.no" src/`
  </acceptance_criteria>
  <done>DEPS-04 implemented: TypeScript at 6.0.3; full Phase 1 target stack (kit 2.59 + vite 8 + ts 6) builds clean; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry ↔ local install | yarn fetches typescript tarball from registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | yarn add typescript@6.0.3 ([ASSUMED]) | mitigate | Blocking human-verify checkpoint confirms Microsoft publisher on npmjs.com before install |
| T-01-04 | Tampering | tsconfig.json edits | mitigate | Acceptance criteria pin the only allowed modification (adding a `types` array); other compilerOptions fields locked to current values |
</threat_model>

<verification>
1. `yarn check` exits 0
2. `yarn build` exits 0
3. `yarn lint` exits 0
4. `yarn list --depth=0 2>/dev/null | grep "typescript@6"` returns a match
5. All Phase 1 cleanup grep gates from Plan 01 still pass (no regression)
</verification>

<success_criteria>
- DEPS-04 satisfied — typescript at 6.0.3
- Phase 1 goal met: app builds cleanly on SvelteKit 2.59 + Vite 8 + TypeScript 6 stack with all dead code removed
- All Phase 1 grep-based success criteria from ROADMAP still hold
</success_criteria>

<output>
Create `.planning/phases/01-cleanup-dependencies/01-05-SUMMARY.md` when done.
</output>
