---
phase: 02-security-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/server/graphql/client.ts
  - src/lib/server/graphql/queries.ts
  - src/routes/+page.server.ts
  - src/routes/skyttere/+page.server.ts
  - src/routes/premieliste/+page.server.ts
  - src/lib/index.ts
  - src/lib/graphql/client.ts
  - src/lib/graphql/queries.ts
  - src/routes/+page.ts
  - src/routes/skyttere/+page.ts
  - src/routes/premieliste/+page.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - DEBT-02
tags:
  - sveltekit
  - server-loaders
  - graphql
  - security
must_haves:
  truths:
    - "AUTH_TOKEN is absent from the client bundle after yarn build"
    - "All three routes load shooter/distinction data via server-side loaders only"
    - "A non-digit ?c= query value (e.g. ?c=abc) loads the page successfully using DEFAULT_CLUB_ID"
    - "premieliste/+page.server.ts returns an explicit error field on GraphQL failure"
    - "$lib/index.ts no longer re-exports getShootersByClub"
  artifacts:
    - path: "src/lib/server/graphql/client.ts"
      provides: "Server-only GraphQL client with AUTH_TOKEN from $env/static/private"
      contains: "$env/static/private"
    - path: "src/lib/server/graphql/queries.ts"
      provides: "getShootersByClub, getShooterWithDistinctions, getShootersWithDistinctions exported from server-only module"
      contains: "export async function getShootersByClub"
    - path: "src/routes/+page.server.ts"
      provides: "PageServerLoad with club ID validation"
      contains: "PageServerLoad"
    - path: "src/routes/skyttere/+page.server.ts"
      provides: "PageServerLoad with club ID validation"
      contains: "PageServerLoad"
    - path: "src/routes/premieliste/+page.server.ts"
      provides: "PageServerLoad with club ID validation and error field"
      contains: "error: error instanceof Error"
  key_links:
    - from: "src/lib/server/graphql/client.ts"
      to: "$env/static/private"
      via: "named import"
      pattern: "import \\{ AUTH_TOKEN \\} from '\\$env/static/private'"
    - from: "src/routes/+page.server.ts"
      to: "$lib/server/graphql/queries"
      via: "named import"
      pattern: "from '\\$lib/server/graphql/queries'"
    - from: "src/routes/+page.server.ts"
      to: "club ID validation"
      via: "regex test"
      pattern: "/\\^\\\\d\\+\\$/\\.test"
---

<objective>
## Phase Goal

**As a** Stordalen Skytterlag member with a phone at the range, **I want to** load the schedule, shooters, and prize pages without my club's auth token ever reaching my browser, **so that** the token cannot be extracted from devtools by other range visitors who inspect the PWA.

This plan is the first vertical slice of Phase 2: deliver end-to-end server-side data fetching. After this plan, the auth token is server-only, club ID input is validated, and the premieliste loader returns an explicit error signal. All three routes continue to render correctly with their existing `+page.svelte` files (rune migration follows in Plan 02 — `data.shooters` and `data.error` props remain shape-compatible).

Purpose: Close T-2-01 (auth token in client bundle, HIGH severity) and T-2-02 (club ID injection, MEDIUM). Eliminate DEBT-02 (silent failure in premieliste).

Output: New `src/lib/server/graphql/` module, three `+page.server.ts` loaders, deleted legacy files, updated barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security-tech-debt/02-CONTEXT.md
@.planning/phases/02-security-tech-debt/02-RESEARCH.md
@.planning/phases/02-security-tech-debt/02-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- Key contracts extracted from the codebase the executor needs -->

From `src/lib/graphql/types.ts` (stays at this path — pure types, client-safe):
- Exported types: `Shot`, `Series`, `Event`, `Shooter`, `Distinction`, `ShooterWithDistinctions`
- Response types used by queries: `GetShooterByClubResponse`, `GetShooterResponse`

From `src/lib/constants.ts`:
- `export const DEFAULT_CLUB_ID = '10782'`

From `src/lib/graphql/queries.ts` (current — to be migrated verbatim except imports):
- `export async function getShootersByClub(clubId: string)` — returns `Shooter[]`
- `export async function getShooterWithDistinctions(organizationId: string): Promise<ShooterWithDistinctions>`
- `export async function getShootersWithDistinctions(clubId: string)` — returns `ShooterWithDistinctions[]`

From `src/lib/graphql/client.ts` (current — to be migrated with env source change):
- `export const graphqlClient = new GraphQLClient(endpoint, { headers: { Authorization: \`Bearer ${import.meta.env.VITE_AUTH_TOKEN}\` } })`

`PageServerLoad` type is auto-generated by SvelteKit from `./$types` after the `+page.server.ts` file exists.

`$env/static/private` is the SvelteKit-provided module — no install needed. Add `AUTH_TOKEN` to `.env` (local) and rename `VITE_AUTH_TOKEN` → `AUTH_TOKEN` in Vercel dashboard (deployment step — documented in a code comment, not automated).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create server-only GraphQL module</name>
  <files>src/lib/server/graphql/client.ts, src/lib/server/graphql/queries.ts</files>
  <read_first>
    - src/lib/graphql/client.ts (current client — copy structure, change env source)
    - src/lib/graphql/queries.ts (all three query functions to migrate verbatim)
    - src/lib/graphql/types.ts (verify which types are imported by queries)
    - .planning/phases/02-security-tech-debt/02-PATTERNS.md (Pattern Assignments for `src/lib/server/graphql/client.ts` and `src/lib/server/graphql/queries.ts`)
    - .planning/phases/02-security-tech-debt/02-RESEARCH.md (Pattern 1; Pitfall 5)
  </read_first>
  <action>
    Create new directory `src/lib/server/graphql/` with two files implementing D-01 and D-02.

    File `src/lib/server/graphql/client.ts`:
    - First lines must be a comment documenting D-04: Vercel env var must be renamed from `VITE_AUTH_TOKEN` to `AUTH_TOKEN` before deploy.
    - Import `GraphQLClient` from `graphql-request`.
    - Import `AUTH_TOKEN` from `$env/static/private` (named import).
    - Declare `const endpoint = 'https://leonls.kongsberg-ts.no/api'`.
    - Export `graphqlClient = new GraphQLClient(endpoint, { headers: { Authorization: \`Bearer ${AUTH_TOKEN}\`, 'Content-Type': 'application/json' } })`.
    - Do NOT use `import.meta.env.VITE_*` anywhere in this file.
    - Tabs for indentation, single quotes, no trailing commas, 100-char width per project conventions in CLAUDE.md.

    File `src/lib/server/graphql/queries.ts`:
    - Copy the entire contents of `src/lib/graphql/queries.ts` verbatim with only these import changes:
      - `from './types'` → `from '$lib/graphql/types'` (types stay at old location)
      - `from './client'` stays unchanged (relative path resolves to the new sibling `client.ts`)
    - Preserve all three exports: `getShootersByClub`, `getShooterWithDistinctions`, `getShootersWithDistinctions`.
    - Preserve the existing per-shooter `try/catch` with `console.warn` inside `getShootersWithDistinctions` — do NOT alter that error-suppression logic; only the outer migration applies here.

    Do NOT delete the old files in this task — deletion happens in Task 3 after loaders are wired.
  </action>
  <verify>
    <automated>test -f src/lib/server/graphql/client.ts &amp;&amp; test -f src/lib/server/graphql/queries.ts &amp;&amp; grep -q "from '\$env/static/private'" src/lib/server/graphql/client.ts &amp;&amp; ! grep -q "VITE_AUTH_TOKEN" src/lib/server/graphql/client.ts &amp;&amp; grep -q "export async function getShootersByClub" src/lib/server/graphql/queries.ts &amp;&amp; grep -q "export async function getShooterWithDistinctions" src/lib/server/graphql/queries.ts &amp;&amp; grep -q "export async function getShootersWithDistinctions" src/lib/server/graphql/queries.ts &amp;&amp; grep -q "Vercel" src/lib/server/graphql/client.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/server/graphql/client.ts` exists
    - `src/lib/server/graphql/queries.ts` exists
    - `src/lib/server/graphql/client.ts` contains the string `import { AUTH_TOKEN } from '$env/static/private'`
    - `src/lib/server/graphql/client.ts` contains the string `Bearer ${AUTH_TOKEN}` (template literal in headers)
    - `src/lib/server/graphql/client.ts` does NOT contain the string `VITE_AUTH_TOKEN`
    - `src/lib/server/graphql/client.ts` does NOT contain the string `import.meta.env`
    - `src/lib/server/graphql/client.ts` contains a comment mentioning Vercel and AUTH_TOKEN
    - `src/lib/server/graphql/queries.ts` exports the three named functions listed above
    - `src/lib/server/graphql/queries.ts` imports types from `$lib/graphql/types`
    - `src/lib/server/graphql/queries.ts` imports `graphqlClient` from `./client`
    - `yarn check` passes (run after this task)
  </acceptance_criteria>
  <done>Server-only GraphQL module exists with `$env/static/private` and the three exports; old files still present and untouched; `yarn check` is green.</done>
</task>

<task type="auto">
  <name>Task 2: Rename three loaders to +page.server.ts and add SEC-02 validation + DEBT-02 error field</name>
  <files>src/routes/+page.server.ts, src/routes/skyttere/+page.server.ts, src/routes/premieliste/+page.server.ts</files>
  <read_first>
    - src/routes/+page.ts (current schedule loader — preserve try/catch shape)
    - src/routes/skyttere/+page.ts (current shooters loader — same shape)
    - src/routes/premieliste/+page.ts (current premieliste loader — currently missing error field on catch)
    - src/lib/constants.ts (DEFAULT_CLUB_ID location)
    - .planning/phases/02-security-tech-debt/02-PATTERNS.md (target patterns for each of the three server loaders, lines on `+page.server.ts` and `premieliste/+page.server.ts`)
    - .planning/phases/02-security-tech-debt/02-RESEARCH.md (Pattern 2, Pattern 7, Pitfall 2)
  </read_first>
  <action>
    Create three new `+page.server.ts` files. Do NOT delete the corresponding `+page.ts` files yet — deletion happens in Task 3 after type-check confirms the new loaders compile.

    Common shape for all three (apply project conventions: tabs, single quotes, no trailing commas, no `any`):
    - Type: `import type { PageServerLoad } from './$types'`
    - Constants import: `import { DEFAULT_CLUB_ID } from '$lib/constants'`
    - Query import target: `$lib/server/graphql/queries` (NOT `$lib`, NOT `$lib/graphql/queries`)
    - Loader body:
      - `const raw = url.searchParams.get('c') || DEFAULT_CLUB_ID`
      - `const clubId = /^\d+$/.test(raw) ? raw : DEFAULT_CLUB_ID` — implements SEC-02 with silent fallback per CONTEXT.md Claude's-Discretion note
      - `try { ... } catch (error) { console.error('...', error); return { ..., error: error instanceof Error ? error.message : 'Unknown error occurred', clubId } }`

    File `src/routes/+page.server.ts`:
    - Import `getShootersByClub` from `$lib/server/graphql/queries`
    - In try: `const shooters = await getShootersByClub(clubId); return { shooters, clubId }`
    - In catch: `return { shooters: null, error: ..., clubId }`
    - This matches the current `+page.ts` shape with `Load` swapped for `PageServerLoad` and SEC-02 validation added.

    File `src/routes/skyttere/+page.server.ts`:
    - Identical body to `+page.server.ts` above (same query, same return shape).

    File `src/routes/premieliste/+page.server.ts`:
    - Import `getShootersWithDistinctions` from `$lib/server/graphql/queries`
    - In try: `const shootersWithDistinctions = await getShootersWithDistinctions(clubId); return { shootersWithDistinctions, clubId, error: null }` — note explicit `error: null` so the type union is consistent.
    - In catch (DEBT-02 fix): `return { shootersWithDistinctions: [], clubId, error: error instanceof Error ? error.message : 'Unknown error occurred' }` — current `+page.ts` is missing this `error` field; add it.
    - Console log prefix: `'Error loading distinctions:'`.

    Do NOT modify any `+page.svelte` file in this task. The existing svelte files reference `data.shooters`, `data.error`, `data.shootersWithDistinctions` — shapes are preserved (the only addition is the explicit `error` field in premieliste success case, which `+page.svelte` does not currently consume but will be available for the rune migration in Plan 02).
  </action>
  <verify>
    <automated>test -f src/routes/+page.server.ts &amp;&amp; test -f src/routes/skyttere/+page.server.ts &amp;&amp; test -f src/routes/premieliste/+page.server.ts &amp;&amp; grep -lq "PageServerLoad" src/routes/+page.server.ts src/routes/skyttere/+page.server.ts src/routes/premieliste/+page.server.ts &amp;&amp; grep -q "/\^\\\\d+\$/\.test" src/routes/+page.server.ts &amp;&amp; grep -q "/\^\\\\d+\$/\.test" src/routes/skyttere/+page.server.ts &amp;&amp; grep -q "/\^\\\\d+\$/\.test" src/routes/premieliste/+page.server.ts &amp;&amp; grep -q "error: error instanceof Error" src/routes/premieliste/+page.server.ts &amp;&amp; grep -q "\$lib/server/graphql/queries" src/routes/+page.server.ts &amp;&amp; yarn check</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist: `src/routes/+page.server.ts`, `src/routes/skyttere/+page.server.ts`, `src/routes/premieliste/+page.server.ts`
    - Each file imports `PageServerLoad` from `./$types`
    - Each file imports its query function from `$lib/server/graphql/queries` (NOT from `$lib`, NOT from `$lib/graphql/queries`)
    - Each file contains the regex `/^\d+$/.test(` (SEC-02)
    - Each file uses `DEFAULT_CLUB_ID` from `$lib/constants` as fallback
    - `src/routes/premieliste/+page.server.ts` returns `error` field in BOTH the try-success branch (`error: null`) and catch branch
    - `src/routes/+page.server.ts` and `src/routes/skyttere/+page.server.ts` return `shooters`, `clubId`, and `error` (in catch) matching the existing `+page.ts` shape
    - No file contains `import.meta.env.VITE_*`
    - `yarn check` passes (PageServerLoad return types are consistent)
    - Old `+page.ts` files are NOT yet deleted (they coexist briefly until Task 3)
  </acceptance_criteria>
  <done>Three server loaders exist with PageServerLoad type, SEC-02 validation, and DEBT-02 error field in premieliste; `yarn check` passes.</done>
</task>

<task type="auto">
  <name>Task 3: Delete legacy files, update barrel, verify end-to-end build</name>
  <files>src/lib/index.ts, src/lib/graphql/client.ts, src/lib/graphql/queries.ts, src/routes/+page.ts, src/routes/skyttere/+page.ts, src/routes/premieliste/+page.ts</files>
  <read_first>
    - src/lib/index.ts (current barrel — must remove the queries re-export)
    - .planning/phases/02-security-tech-debt/02-PATTERNS.md (target pattern for `src/lib/index.ts`; "Files to Delete After Migration" section)
    - .planning/phases/02-security-tech-debt/02-RESEARCH.md (Pitfall 1: barrel must not re-export server-only code; Pitfall 5: env var rename)
  </read_first>
  <action>
    Finalize the migration: delete legacy files and update the barrel so the codebase has exactly one source of truth for GraphQL.

    1. Update `src/lib/index.ts` per D-02:
       - Remove the line `export { getShootersByClub } from './graphql/queries';`
       - Keep the file (do not delete) but leave only comments:
         - Line 1: `// place files you want to import through the $lib alias in this folder.`
         - Line 2: `// Query functions are server-only — import from $lib/server/graphql/queries in +page.server.ts files.`
       - The file remains a no-export module; this avoids accidentally tainting `$lib` with server-only imports (Pitfall 1).

    2. Delete the following legacy files (use `rm` or equivalent — they are now superseded):
       - `src/lib/graphql/client.ts` (superseded by `src/lib/server/graphql/client.ts`)
       - `src/lib/graphql/queries.ts` (superseded by `src/lib/server/graphql/queries.ts`)
       - `src/routes/+page.ts` (superseded by `+page.server.ts`)
       - `src/routes/skyttere/+page.ts` (superseded by `+page.server.ts`)
       - `src/routes/premieliste/+page.ts` (superseded by `+page.server.ts`)

    3. Confirm `src/lib/graphql/types.ts` still exists and is unchanged — it stays at this path because types are client-safe and `+page.svelte` files import from `$lib/graphql/types`.

    4. Run `yarn check && yarn build` and verify:
       - Type-check passes
       - Build emits a client bundle to `.svelte-kit/output/client/`
       - `grep -r "AUTH_TOKEN" .svelte-kit/output/client/` returns zero results (SEC-01 success criterion #1)

    If `yarn check` reports any import error like "Cannot import $lib/server/... into code that runs in the browser" (Pitfall 1 warning sign), STOP and inspect — it means a non-server file is importing from `$lib/server/`. The fix is to import directly from `$lib/server/graphql/queries` only in `+page.server.ts` / `$lib/server/**` files.

    If `yarn check` reports a type error referencing `+page.svelte` and `PageData`, this is expected ONLY in `src/routes/skyttere/+page.svelte` due to its broken `../skyttere/$types` import path noted in Pitfall 2. Do NOT fix that here — it is owned by Plan 02 (rune migration). If the error blocks the build, temporarily change that one import to `./$types` so the build passes; Plan 02 will own the full rune migration of that file.
  </action>
  <verify>
    <automated>! test -f src/lib/graphql/client.ts &amp;&amp; ! test -f src/lib/graphql/queries.ts &amp;&amp; ! test -f src/routes/+page.ts &amp;&amp; ! test -f src/routes/skyttere/+page.ts &amp;&amp; ! test -f src/routes/premieliste/+page.ts &amp;&amp; test -f src/lib/graphql/types.ts &amp;&amp; ! grep -q "export.*getShootersByClub" src/lib/index.ts &amp;&amp; yarn check &amp;&amp; yarn build &amp;&amp; ! grep -r "AUTH_TOKEN" .svelte-kit/output/client/ &amp;&amp; echo "SEC-01 PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/graphql/client.ts` does NOT exist
    - `src/lib/graphql/queries.ts` does NOT exist
    - `src/routes/+page.ts` does NOT exist
    - `src/routes/skyttere/+page.ts` does NOT exist
    - `src/routes/premieliste/+page.ts` does NOT exist
    - `src/lib/graphql/types.ts` DOES still exist (types are client-safe, stay put)
    - `src/lib/index.ts` does NOT contain `export { getShootersByClub`
    - `src/lib/index.ts` contains a comment mentioning `$lib/server/graphql/queries`
    - `yarn check` exits 0
    - `yarn build` exits 0 and emits files under `.svelte-kit/output/client/`
    - `grep -r "AUTH_TOKEN" .svelte-kit/output/client/` exits 1 (no matches) — this is the SEC-01 acceptance gate from ROADMAP.md
    - No file under `src/` contains `VITE_AUTH_TOKEN` or `import.meta.env.VITE_AUTH_TOKEN`
  </acceptance_criteria>
  <done>Legacy GraphQL files deleted, barrel cleaned, full build green, `AUTH_TOKEN` absent from client bundle — SEC-01 acceptance gate satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server loader (`+page.server.ts`) | Untrusted `?c=` query param crosses here; URL is fully attacker-controlled |
| server loader → leonls.kongsberg-ts.no GraphQL | `AUTH_TOKEN` injected as Bearer header on server; never crosses back to browser |
| build process → `.svelte-kit/output/client/` | Build-time enforcement: `$lib/server/` modules must not be reachable from client entry points |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Information Disclosure | GraphQL auth token in client JS bundle | mitigate | Move client to `src/lib/server/graphql/client.ts`; read `AUTH_TOKEN` via `$env/static/private`; SvelteKit static analysis blocks any client import of `$lib/server/`. Acceptance: `grep -r "AUTH_TOKEN" .svelte-kit/output/client/` returns zero matches after `yarn build` (ROADMAP success criterion #1). |
| T-2-02 | Tampering | `?c=` URL query param injected into GraphQL variable | mitigate | Validate raw value with `/^\d+$/.test(raw)`; on failure silently fall back to `DEFAULT_CLUB_ID` per CONTEXT.md Claude-discretion decision. Note: `graphql-request` uses parameterized variables (no string interpolation), so a malformed value cannot escape the GraphQL query envelope — the regex is defense-in-depth against API-side surprises. |
| T-2-03 | Information Disclosure | Vercel env var `VITE_AUTH_TOKEN` still set at deploy time (Pitfall 5) | accept (operational) | Code-side change is complete; the manual Vercel dashboard rename is documented in a comment at the top of `src/lib/server/graphql/client.ts`. The user (Adrian) owns the dashboard step. Not blocking phase verification because Phase 2 success criteria are build-artifact based. |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase per RESEARCH.md "Package Legitimacy Audit" section; section explicitly "not applicable". Package legitimacy gate does not apply. |
</threat_model>

<verification>
- `yarn check` passes after every task
- `yarn build` passes after Task 3
- `grep -r "AUTH_TOKEN" .svelte-kit/output/client/` returns zero matches (SEC-01 — ROADMAP success criterion 1)
- `grep -r "VITE_AUTH_TOKEN" src/` returns zero matches
- Manual: visit `/?c=abc` in dev server → page loads with default club shooter data (SEC-02 — ROADMAP success criterion 2)
- Manual: visit `/premieliste?c=99999999` → either real data renders or the page renders gracefully (no crash); a network-induced failure now returns `data.error` rather than empty array (DEBT-02 — ROADMAP success criterion 4)
</verification>

<success_criteria>
- All five legacy files (3 loaders + client.ts + queries.ts) deleted
- New module `src/lib/server/graphql/` contains `client.ts` and `queries.ts`
- Three `+page.server.ts` loaders exist with `PageServerLoad` type and `/^\d+$/` validation
- `$lib/index.ts` no longer exports `getShootersByClub`
- `yarn check && yarn build` exits 0
- `AUTH_TOKEN` absent from `.svelte-kit/output/client/` (SEC-01 verified)
- premieliste loader returns `error` field on catch (DEBT-02 verified by `yarn check` PageData typing)
</success_criteria>

<output>
Create `.planning/phases/02-security-tech-debt/02-01-SUMMARY.md` when done. Include: list of created/deleted files, the SEC-01 grep result, any `yarn check` warnings encountered, and a note for Adrian about the required Vercel dashboard rename (`VITE_AUTH_TOKEN` → `AUTH_TOKEN`) before next deploy.
</output>
