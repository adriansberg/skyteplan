---
phase: 03-ux-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/+layout.svelte
  - src/lib/components/BottomTabBar.svelte
autonomous: true
requirements: [UX-01]

must_haves:
  truths:
    - "A fixed bottom tab bar with three tabs (Skyteplan / Skyttere / Premieliste) is visible on all three routes"
    - "The bottom tab bar is 56px tall (h-14) and adds env(safe-area-inset-bottom) padding"
    - "The active tab is indicated by a 2px emerald-600 top border and emerald-600 label text; inactive tabs use neutral-500 text"
    - "The top bar is a slim 40px (h-10) sticky header containing only the logo image and RefreshButton — no nav links and no site name text"
    - "Page content does not disappear behind the fixed bottom tab bar (pb-16 wrapper applied at layout level)"
  artifacts:
    - path: "src/lib/components/BottomTabBar.svelte"
      provides: "Fixed bottom navigation with three tabs"
      contains: "fixed bottom-0"
    - path: "src/routes/+layout.svelte"
      provides: "Slim sticky top bar + BottomTabBar mount + pb-16 children wrapper"
      contains: "BottomTabBar"
  key_links:
    - from: "src/routes/+layout.svelte"
      to: "src/lib/components/BottomTabBar.svelte"
      via: "import + <BottomTabBar /> mount after PullToRefresh"
      pattern: "import BottomTabBar.*BottomTabBar.svelte"
    - from: "src/lib/components/BottomTabBar.svelte"
      to: "$app/state page"
      via: "$derived(page.url.pathname) active-route detection"
      pattern: "page.url.pathname"
---

<objective>
Replace the top-nav-with-emoji-pills with a slim 40px logo+refresh top bar plus a new fixed bottom tab bar at 56px (UX-01). Layout wraps `{@render children?.()}` in `pb-16` so content clears the bottom tabs.

Purpose: Mobile-first one-handed reachability at the range. Bottom nav puts navigation in thumb range and frees the top bar for branding only.

Output: Modified `+layout.svelte`, new `BottomTabBar.svelte`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-ux-redesign/03-CONTEXT.md
@.planning/phases/03-ux-redesign/03-UI-SPEC.md
@.planning/phases/03-ux-redesign/03-PATTERNS.md

<interfaces>
<!-- Active-route detection pattern already exists in src/routes/+layout.svelte:13-15.
     BottomTabBar reuses the same Svelte 5 runes pattern with $app/state. -->

From src/routes/+layout.svelte (current pattern to preserve in script block):
- `import { page } from '$app/state';`
- `const isSchedulePage = $derived(page.url.pathname === '/');`
- `const isShootersPage = $derived(page.url.pathname === '/skyttere');`
- `const isPremielistePage = $derived(page.url.pathname === '/premieliste');`

Existing components mounted in layout (preserve):
- `RefreshButton` from `$lib/components/RefreshButton.svelte` — moves into slim top bar
- `PullToRefresh` from `$lib/components/PullToRefresh.svelte` — stays at end of layout
- `InstallPrompt` from `$lib/components/InstallPrompt.svelte` — stays at end of layout

env(safe-area-inset-bottom): NOT a Tailwind utility. Must be applied as inline `style="padding-bottom: env(safe-area-inset-bottom)"` per UI-SPEC.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create BottomTabBar.svelte component</name>
  <files>src/lib/components/BottomTabBar.svelte</files>
  <read_first>
    - src/routes/+layout.svelte (canonical active-route + Svelte 5 runes pattern to mirror, lines 1-21)
    - src/lib/components/EventStatusBadge.svelte (canonical Svelte 5 runes structure)
    - .planning/phases/03-ux-redesign/03-UI-SPEC.md (Bottom Tab Bar section + tab anatomy table)
    - .planning/phases/03-ux-redesign/03-PATTERNS.md (BottomTabBar.svelte section)
  </read_first>
  <behavior>
    - Active tab on `/` route: emerald-600 top border + emerald-600 text, label "Skyteplan"
    - Active tab on `/skyttere` route: emerald-600 top border + emerald-600 text, label "Skyttere"
    - Active tab on `/premieliste` route: emerald-600 top border + emerald-600 text, label "Premieliste"
    - Inactive tabs: transparent top border + neutral-500 text
    - Component renders as a single `<nav>` element with three `<a>` children
  </behavior>
  <action>
    Create new file `src/lib/components/BottomTabBar.svelte`. Component takes no props. Script block uses Svelte 5 runes only: `import { page } from '$app/state'` and three `$derived` constants — `isSchedulePage` ($derived(page.url.pathname === '/')), `isShootersPage` ($derived(page.url.pathname === '/skyttere')), `isPremielistePage` ($derived(page.url.pathname === '/premieliste')). Mirror the exact pattern in `+layout.svelte` lines 4 and 13-15.

    Template: a single `<nav>` element with classes `fixed bottom-0 left-0 right-0 z-50 flex h-14 border-t border-neutral-200 bg-neutral-50` and inline attribute `style="padding-bottom: env(safe-area-inset-bottom)"`. Inside, three `<a>` tabs in order: `href="/"` (Skyteplan), `href="/skyttere"` (Skyttere), `href="/premieliste"` (Premieliste). Plain Norwegian text labels, no emojis, no SVG icons.

    Each `<a>` uses class `flex-1 flex flex-col items-center justify-center border-t-2 text-xs font-medium` and a ternary class block that resolves to `border-emerald-600 text-emerald-600` when active and `border-transparent text-neutral-500` when inactive — gated by the corresponding `$derived` boolean. No background fill, no shadows, no gradients, no hover transitions.
  </action>
  <verify>
    <automated>test -f src/lib/components/BottomTabBar.svelte && grep -q "fixed bottom-0 left-0 right-0 z-50 flex h-14" src/lib/components/BottomTabBar.svelte && grep -q 'env(safe-area-inset-bottom)' src/lib/components/BottomTabBar.svelte && grep -q "border-emerald-600 text-emerald-600" src/lib/components/BottomTabBar.svelte && grep -q "border-transparent text-neutral-500" src/lib/components/BottomTabBar.svelte && grep -q '\$app/state' src/lib/components/BottomTabBar.svelte && grep -q 'href="/"' src/lib/components/BottomTabBar.svelte && grep -q 'href="/skyttere"' src/lib/components/BottomTabBar.svelte && grep -q 'href="/premieliste"' src/lib/components/BottomTabBar.svelte && grep -q 'Skyteplan' src/lib/components/BottomTabBar.svelte && grep -q 'Skyttere' src/lib/components/BottomTabBar.svelte && grep -q 'Premieliste' src/lib/components/BottomTabBar.svelte && yarn check</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `src/lib/components/BottomTabBar.svelte`
    - File contains string `fixed bottom-0 left-0 right-0 z-50 flex h-14`
    - File contains string `border-t border-neutral-200 bg-neutral-50`
    - File contains string `style="padding-bottom: env(safe-area-inset-bottom)"`
    - File contains string `border-emerald-600 text-emerald-600`
    - File contains string `border-transparent text-neutral-500`
    - File contains imports `import { page } from '$app/state'`
    - File contains three `$derived(page.url.pathname === ...)` constants for `/`, `/skyttere`, `/premieliste`
    - File contains three `<a>` elements with `href="/"`, `href="/skyttere"`, `href="/premieliste"`
    - File contains plain text labels `Skyteplan`, `Skyttere`, `Premieliste`
    - File does NOT contain any emoji characters (no 📅, 👥, 🏆)
    - File does NOT contain `export let`, `$:`, or `<svelte:options>`
    - File does NOT contain `shadow-`, `bg-gradient-`, or `bg-blue-100` classes
    - `yarn check` exits 0
  </acceptance_criteria>
  <done>BottomTabBar.svelte exists, renders three Norwegian text tabs in a fixed bottom nav with active-route emerald-600 indicator and safe-area-aware bottom padding, and the project type-checks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace top nav in +layout.svelte with slim header and mount BottomTabBar</name>
  <files>src/routes/+layout.svelte</files>
  <read_first>
    - src/routes/+layout.svelte (current top-nav block lines 27-64 to replace; children render line 66; existing PullToRefresh/InstallPrompt mount lines 69-72)
    - src/lib/components/BottomTabBar.svelte (component created in Task 1 — verify import path)
    - .planning/phases/03-ux-redesign/03-UI-SPEC.md (Top Bar section + Page Content Wrapper section)
    - .planning/phases/03-ux-redesign/03-PATTERNS.md (+layout.svelte target header section)
  </read_first>
  <action>
    Edit `src/routes/+layout.svelte`. Preserve the existing `<script lang="ts">` block unchanged (imports, `let { children } = $props()`, the three `$derived` route-flags, and the `browser` PWA dynamic import on lines 1-21) — but ADD a new import `import BottomTabBar from '$lib/components/BottomTabBar.svelte';` alongside the other component imports.

    Replace the entire `<header>` block (lines 27-64) with a slim header: a sticky `<header>` with classes `sticky top-0 z-40 h-10 border-b border-neutral-200 bg-neutral-50 flex items-center justify-between px-4`. Inside the header: a single `<a href="/">` wrapping `<img src={stordalenLogo} alt="Stordalen Skytterlag" class="h-8 w-auto" />`, followed by `<RefreshButton />`. No site-name `<span>`, no nav links, no emoji, no `transition-opacity`, no `hover:` classes on the logo link.

    Replace the bare `{@render children?.()}` (line 66) with a wrapping `<div class="pb-16">{@render children?.()}</div>` so all page content sits above the 56px fixed bottom tab bar.

    After `<PullToRefresh />` and before `<InstallPrompt />` (line 70 region), add `<BottomTabBar />`. Keep `PullToRefresh` and `InstallPrompt` order otherwise unchanged.

    Remove the three `$derived` route-flag constants (`isSchedulePage`, `isShootersPage`, `isPremielistePage`) from the script block — they were used only by the now-removed top-nav pills. Keep the `import { page } from '$app/state'` only if still referenced; otherwise remove that import too. (Verify with grep before deciding.)
  </action>
  <verify>
    <automated>grep -q "sticky top-0 z-40 h-10 border-b border-neutral-200 bg-neutral-50" src/routes/+layout.svelte && grep -q "import BottomTabBar from '\$lib/components/BottomTabBar.svelte'" src/routes/+layout.svelte && grep -q "<BottomTabBar />" src/routes/+layout.svelte && grep -q '<div class="pb-16">' src/routes/+layout.svelte && ! grep -q '📅 Skyteplan' src/routes/+layout.svelte && ! grep -q '👥 Skyttere' src/routes/+layout.svelte && ! grep -q '🏆 Premier' src/routes/+layout.svelte && ! grep -q 'bg-blue-100 text-blue-800' src/routes/+layout.svelte && ! grep -q 'Stordalen Skytterlag\s*</span>' src/routes/+layout.svelte && yarn check && yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `src/routes/+layout.svelte` contains string `sticky top-0 z-40 h-10 border-b border-neutral-200 bg-neutral-50`
    - `src/routes/+layout.svelte` contains import `import BottomTabBar from '$lib/components/BottomTabBar.svelte'`
    - `src/routes/+layout.svelte` contains `<BottomTabBar />` mount
    - `src/routes/+layout.svelte` contains wrapper `<div class="pb-16">` around `{@render children?.()}`
    - `src/routes/+layout.svelte` contains `<RefreshButton />` inside the header
    - `src/routes/+layout.svelte` does NOT contain emoji `📅`, `👥`, or `🏆`
    - `src/routes/+layout.svelte` does NOT contain class `bg-blue-100 text-blue-800` (old active-pill style)
    - `src/routes/+layout.svelte` does NOT contain visible site-name text "Stordalen Skytterlag" inside a `<span>` (still allowed as `alt` attribute on the logo image)
    - `src/routes/+layout.svelte` does NOT contain any nav `<a>` to `/skyttere` or `/premieliste` (those routes are reached only via BottomTabBar)
    - `yarn check` exits 0
    - `yarn build` exits 0
  </acceptance_criteria>
  <done>Layout shows a 40px slim top bar with logo+refresh only, mounts BottomTabBar above InstallPrompt, wraps children in pb-16, type-checks, and builds cleanly.</done>
</task>

</tasks>

<verification>
- `yarn check` — zero TypeScript or Svelte errors
- `yarn build` — production build succeeds (`@sveltejs/adapter-vercel` output produced)
- Bottom tab bar appears on all three routes: `/`, `/skyttere`, `/premieliste` (verified visually in Plan 03 wave by execute-phase)
- No emoji and no `bg-blue-100`/`bg-gradient-` classes remain in `+layout.svelte` (grep gates above)
</verification>

<success_criteria>
1. `src/lib/components/BottomTabBar.svelte` exists and matches Bottom Tab Bar contract in 03-UI-SPEC.md
2. `+layout.svelte` mounts BottomTabBar and wraps children with `pb-16`
3. Top bar is 40px tall, contains only logo image + RefreshButton, no site-name text, no nav links
4. Project type-checks and builds
</success_criteria>

<output>
Create `.planning/phases/03-ux-redesign/03-01-SUMMARY.md` when done.
</output>
