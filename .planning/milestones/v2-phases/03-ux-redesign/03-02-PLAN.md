---
phase: 03-ux-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/components/EventStatusBadge.svelte
autonomous: true
requirements: [UX-03]

must_haves:
  truths:
    - "ongoing status renders a solid emerald-600 pill with white text and animate-pulse, labelled 'Pågår'"
    - "completed status renders a solid slate-600 pill with white text, labelled 'Ferdig'"
    - "upcoming status renders a solid amber-500 pill with white text, labelled 'Kommende'"
    - "did_not_start status renders a solid gray-400 pill with white text, labelled 'Møtte ikke'"
    - "No emoji or check/cross symbols appear inside any badge"
  artifacts:
    - path: "src/lib/components/EventStatusBadge.svelte"
      provides: "Solid-fill status badge with Norwegian labels"
      contains: "Pågår"
  key_links:
    - from: "src/lib/components/EventStatusBadge.svelte"
      to: "src/lib/utils/helpers.ts getEventStatus"
      via: "$derived(getEventStatus(event))"
      pattern: "getEventStatus"
---

<objective>
Replace tinted/symbol badges in `EventStatusBadge.svelte` with solid-fill colour pills carrying Norwegian text labels (UX-03). Script block (Props interface + `$derived(getEventStatus(event))`) is preserved unchanged; only the template `{#if}/{:else if}/{:else}` blocks change.

Purpose: Outdoor legibility — solid fills + plain Norwegian text outperform pastel pills with emoji symbols in bright sun.

Output: Modified `EventStatusBadge.svelte` with four solid-fill variants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ux-redesign/03-CONTEXT.md
@.planning/phases/03-ux-redesign/03-UI-SPEC.md
@.planning/phases/03-ux-redesign/03-PATTERNS.md

<interfaces>
<!-- Existing component is already on Svelte 5 runes. Props interface and $derived stay; only the four template branches change. -->

From src/lib/utils/helpers.ts:
- `getEventStatus(event)` returns one of: `'completed' | 'ongoing' | 'did_not_start' | 'upcoming'`

From src/lib/components/EventStatusBadge.svelte (script block to preserve unchanged):
- `interface Props { event: Event & { shooter: Shooter }; class?: string }`
- `let { event, class: className = '' }: Props = $props();`
- `const status = $derived(getEventStatus(event));`

Badge size (UI-SPEC D-13): `px-2 py-1 text-xs font-medium rounded-full inline-flex items-center` — same for all four variants.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert EventStatusBadge to solid-fill Norwegian variants</name>
  <files>src/lib/components/EventStatusBadge.svelte</files>
  <read_first>
    - src/lib/components/EventStatusBadge.svelte (current full template, lines 1-39)
    - .planning/phases/03-ux-redesign/03-UI-SPEC.md (Color section — full badge color map UX-03)
    - .planning/phases/03-ux-redesign/03-PATTERNS.md (EventStatusBadge.svelte section — target template block)
  </read_first>
  <action>
    Edit `src/lib/components/EventStatusBadge.svelte`. Preserve lines 1-13 unchanged: imports, Props interface, `let { event, class: className = '' }: Props = $props();`, and `const status = $derived(getEventStatus(event));`.

    Replace the four template branches (lines 15-39) so each branch emits a `<span>` with the shared base classes `inline-flex items-center rounded-full px-2 py-1 text-xs font-medium text-white` plus `{className}` interpolation, and a status-specific background:

    - `status === 'completed'`: background `bg-slate-600`, text content `Ferdig` (replaces previous green-tinted `✓`)
    - `status === 'ongoing'`: background `bg-emerald-600`, ALSO include `animate-pulse` class on the span, text content `Pågår` (replaces previous yellow-tinted `🎯`)
    - `status === 'did_not_start'`: background `bg-gray-400`, text content `Møtte ikke` (replaces previous red-tinted `✗`)
    - `else` branch (covers `upcoming`): background `bg-amber-500`, text content `Kommende` (replaces previous blue-tinted `⏱️`)

    No emoji, no symbols, no green/yellow/red/blue-100 backgrounds, no `text-green-800`/`text-yellow-800`/`text-red-800`/`text-blue-800` text colours. Whitespace inside each `<span>` should be trimmed so the rendered label has no leading/trailing whitespace — but follow project Prettier conventions (tabs, single quotes).
  </action>
  <verify>
    <automated>grep -q 'bg-emerald-600' src/lib/components/EventStatusBadge.svelte && grep -q 'bg-slate-600' src/lib/components/EventStatusBadge.svelte && grep -q 'bg-amber-500' src/lib/components/EventStatusBadge.svelte && grep -q 'bg-gray-400' src/lib/components/EventStatusBadge.svelte && grep -q 'Pågår' src/lib/components/EventStatusBadge.svelte && grep -q 'Ferdig' src/lib/components/EventStatusBadge.svelte && grep -q 'Kommende' src/lib/components/EventStatusBadge.svelte && grep -q 'Møtte ikke' src/lib/components/EventStatusBadge.svelte && grep -q 'animate-pulse' src/lib/components/EventStatusBadge.svelte && ! grep -q 'bg-green-100' src/lib/components/EventStatusBadge.svelte && ! grep -q 'bg-yellow-100' src/lib/components/EventStatusBadge.svelte && ! grep -q 'bg-red-100' src/lib/components/EventStatusBadge.svelte && ! grep -q 'bg-blue-100' src/lib/components/EventStatusBadge.svelte && ! grep -q '🎯' src/lib/components/EventStatusBadge.svelte && ! grep -q '⏱' src/lib/components/EventStatusBadge.svelte && yarn check</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/components/EventStatusBadge.svelte` contains classes `bg-emerald-600`, `bg-slate-600`, `bg-amber-500`, `bg-gray-400`
    - File contains Norwegian labels `Pågår`, `Ferdig`, `Kommende`, `Møtte ikke`
    - File contains `animate-pulse` (on the ongoing variant only)
    - File contains `text-white` on every variant span
    - File does NOT contain old tinted background classes `bg-green-100`, `bg-yellow-100`, `bg-red-100`, `bg-blue-100`
    - File does NOT contain old text-color classes `text-green-800`, `text-yellow-800`, `text-red-800`, `text-blue-800`
    - File does NOT contain emoji `🎯`, `⏱`, `✓`, `✗`
    - Script block (lines 1-13) is unchanged: still contains `interface Props`, `let { event, class: className = '' }: Props = $props()`, and `const status = $derived(getEventStatus(event))`
    - `yarn check` exits 0
  </acceptance_criteria>
  <done>EventStatusBadge renders four solid-fill Norwegian pills, ongoing pulses, script block intact, project type-checks.</done>
</task>

</tasks>

<verification>
- `yarn check` — zero TypeScript or Svelte errors
- Grep gates above confirm all four variants present with correct colours/labels, and all old tinted variants and emoji removed
- Badge consumers (`+page.svelte` line 163) need no changes — same `<EventStatusBadge {event} />` API
</verification>

<success_criteria>
1. All four badge variants use solid backgrounds with white text
2. Labels are Norwegian: Pågår, Ferdig, Kommende, Møtte ikke — no emoji, no symbols
3. ongoing variant retains animate-pulse
4. Props interface and `$derived(getEventStatus(event))` script block unchanged
</success_criteria>

<output>
Create `.planning/phases/03-ux-redesign/03-02-SUMMARY.md` when done.
</output>
