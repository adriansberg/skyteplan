---
phase: 03-ux-redesign
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/routes/+page.svelte
  - src/routes/skyttere/+page.svelte
  - src/routes/premieliste/+page.svelte
autonomous: true
requirements: [UX-02, UX-04]

must_haves:
  truths:
    - "All three route pages have all `shadow-md`, `shadow-lg`, `shadow-sm` classes removed"
    - "All three route pages have all `bg-gradient-*` classes removed and replaced with neutral backgrounds"
    - "Schedule page (+page.svelte) date section headers are CSS-sticky at top-10 with bg-neutral-50, and use scroll-mt-10 (no scroll-mt-14/scroll-mt-20)"
    - "Schedule page event times and final scores carry font-mono"
    - "Schedule page data text (event names, shooter names) uses text-base font-semibold or stronger headings"
    - "Shooters page (skyttere/+page.svelte) times and scores carry font-mono; error/empty copy is Norwegian"
    - "Premieliste page shooter cards have no shadow; emojis (🎁, 🏆, 🥇, 🎯) removed from summary section headers and empty state"
  artifacts:
    - path: "src/routes/+page.svelte"
      provides: "Schedule page with sticky date headers, neutral palette, monospace times/scores"
      contains: "sticky top-10"
    - path: "src/routes/skyttere/+page.svelte"
      provides: "Shooters page with neutral palette, monospace times/scores, Norwegian error/empty copy"
      contains: "Feil ved lasting av data:"
    - path: "src/routes/premieliste/+page.svelte"
      provides: "Premieliste page with neutral palette, shadows + emoji removed"
      contains: "border border-gray-200 bg-white p-4"
  key_links:
    - from: "src/routes/+page.svelte sticky date header"
      to: "src/routes/+layout.svelte top bar h-10"
      via: "sticky top-10 matches layout top bar height (40px)"
      pattern: "sticky top-10"
    - from: "src/routes/+page.svelte registerTodaySection action"
      to: "scroll-mt-10 offset on the section wrapper"
      via: "scrollIntoView clears the slim top bar"
      pattern: "scroll-mt-10"
---

<objective>
Apply the outdoor-optimized palette and typography (UX-02) to all three route pages and add CSS-sticky date headers on the schedule page (UX-04). Removes all box shadows, removes all gradient backgrounds, applies neutral palette, sets data text to `text-base font-semibold` with `font-mono` on times/scores, and translates remaining English copy on the Shooters page to Norwegian.

Purpose: One pass per route file keeps each task small and gives the parent reviewer a clean visual diff per page. Plan depends on Wave 1's `h-10` top bar so `sticky top-10` / `scroll-mt-10` line up correctly.

Output: Modified `+page.svelte`, `skyttere/+page.svelte`, `premieliste/+page.svelte`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ux-redesign/03-CONTEXT.md
@.planning/phases/03-ux-redesign/03-UI-SPEC.md
@.planning/phases/03-ux-redesign/03-PATTERNS.md

@.planning/phases/03-ux-redesign/03-01-SUMMARY.md

<interfaces>
<!-- Wave 1 (Plan 01) sets the top bar height to h-10 and adds `pb-16` at layout level.
     pb-16 is therefore NOT required on per-page containers — apply only the typography/shadow/sticky changes here. -->

From src/routes/+page.svelte:
- Date section wrapper currently at line 105-108: `scroll-mt-14 overflow-hidden rounded-lg border border-gray-200 bg-white shadow-md sm:scroll-mt-20 sm:rounded-xl sm:shadow-lg`
- Inner date header currently at line 110-112: `border-b border-gray-200 bg-gradient-to-r from-blue-50 to-indigo-50 px-3 py-3 sm:px-6 sm:py-4`
- Inner heading currently at line 113: `text-lg font-semibold text-gray-900 sm:text-xl`
- Event name heading currently at line 138: `text-sm font-semibold text-gray-900 sm:text-lg`
- Time span currently at line 170: `<span>{formatNorwegianTime(event.checkinDateTime)}</span>` (no font-mono)
- Final score currently at line 174: `<div class="text-lg font-bold text-blue-600">{finalScore}</div>` (no font-mono)
- registerTodaySection action declared at lines 28-42 — keep as-is

From src/routes/skyttere/+page.svelte:
- Loading copy line 22: `Loading shooters data...` → Norwegian
- Error heading line 26: `Error loading data:` → Norwegian
- Error body line 27: `Error: {error}` → Norwegian
- Card wrapper line 65: `overflow-hidden rounded-lg border border-gray-200 bg-white shadow-md sm:rounded-xl sm:shadow-lg`
- Summary wrapper line 69: `bg-gradient-to-r from-blue-50 to-indigo-50 ... hover:from-blue-100 hover:to-indigo-100`
- Time spans lines 175, 130, 215, 295 (uses formatNorwegianTime) need font-mono
- Score spans line 187 (`{lastSeries.sum}`), 237 (`{series.sum}`), 318-320 desktop score need font-mono
- Empty-results copy line 349: `No results yet` → Norwegian
- Empty-events copy line 360: `No events scheduled` → Norwegian
- Preserve `<style>` block lines 370-394 unchanged

From src/routes/premieliste/+page.svelte:
- Shooter card line 110: `rounded-lg border border-gray-200 bg-white p-4 shadow-sm` — remove `shadow-sm`
- Emoji in empty state line 99: `<div class="mb-4 text-4xl">🎯</div>` — remove
- Emoji in summary section headers lines 155 (`🎁`), 183 (`🏆`), 211 (`🥇`) — remove the wrapping `<span class="text-2xl">…</span>`
- Trophy emoji line 132 inside distinction pill `🏆 {distinction.name}` — remove the `🏆 ` prefix string only
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Schedule page — sticky date headers, neutral palette, monospace data</name>
  <files>src/routes/+page.svelte</files>
  <read_first>
    - src/routes/+page.svelte (full file — date section structure lines 100-285)
    - .planning/phases/03-ux-redesign/03-UI-SPEC.md (Sticky Date Headers section + Typography section + Color "Colors to remove" table)
    - .planning/phases/03-ux-redesign/03-PATTERNS.md (+page.svelte section)
    - .planning/phases/03-ux-redesign/03-01-SUMMARY.md (confirm Wave 1 set top bar to h-10)
  </read_first>
  <action>
    Edit `src/routes/+page.svelte`. Keep all script logic, derived state, and the `registerTodaySection` action unchanged.

    Restructure the date section card so the header is sticky and the outer wrapper carries the scroll offset only:
    - Outer `<div use:registerTodaySection={isToday}>` wrapper (currently line 105-108) gets class `scroll-mt-10` ONLY — drop `overflow-hidden rounded-lg border border-gray-200 bg-white shadow-md sm:scroll-mt-20 sm:rounded-xl sm:shadow-lg`. (Removing `overflow-hidden` is required for `position: sticky` to escape the parent.)
    - Inner date-header `<div>` (currently line 110-112) becomes the sticky element: replace its classes with `sticky top-10 z-30 bg-neutral-50 border-b border-neutral-200 px-3 py-3 sm:px-6 sm:py-4`. No gradient, no shadow.
    - Inner `<h2>` (line 113) class becomes `text-xl font-bold text-neutral-900`. The inner count `<span>` class becomes `ml-1 text-xs font-normal text-neutral-600` (drop the `sm:text-sm`).

    Apply data typography:
    - Event name `<h3>` (line 138): change `text-sm font-semibold text-gray-900 sm:text-lg` to `text-base font-semibold text-gray-900 sm:text-lg`.
    - Time `<span>` (line 170): add `font-mono` class so `<span class="font-mono">{formatNorwegianTime(event.checkinDateTime)}</span>`.
    - Final score `<div>` (line 174): change `text-lg font-bold text-blue-600` to `text-lg font-bold text-blue-600 font-mono`.
    - Ongoing score `<div>` (line 181-183): change `text-sm font-medium text-blue-600` to `text-sm font-medium text-blue-600 font-mono`.
    - Sub-event score `<div>` (line 229-231): add `font-mono` to the score `<div class="text-sm font-bold text-blue-600">`.

    DO NOT add `pb-16` to the outer container — the layout wrapper (Plan 01) already supplies it. DO NOT touch the script block, the `$derived` blocks, the `Splash` mount, the error block, the sub-event details block structure, or the existing `EventStatusBadge` placement.
  </action>
  <verify>
    <automated>grep -q 'sticky top-10 z-30 bg-neutral-50' src/routes/+page.svelte && grep -q 'scroll-mt-10' src/routes/+page.svelte && grep -q 'text-xl font-bold text-neutral-900' src/routes/+page.svelte && grep -q 'font-mono' src/routes/+page.svelte && ! grep -q 'scroll-mt-14' src/routes/+page.svelte && ! grep -q 'scroll-mt-20' src/routes/+page.svelte && ! grep -q 'shadow-md' src/routes/+page.svelte && ! grep -q 'shadow-lg' src/routes/+page.svelte && ! grep -q 'bg-gradient-to-r from-blue-50 to-indigo-50' src/routes/+page.svelte && yarn check</automated>
  </verify>
  <acceptance_criteria>
    - `src/routes/+page.svelte` contains `sticky top-10 z-30 bg-neutral-50 border-b border-neutral-200`
    - `src/routes/+page.svelte` contains `scroll-mt-10`
    - `src/routes/+page.svelte` contains `text-xl font-bold text-neutral-900`
    - `src/routes/+page.svelte` contains `font-mono` (used on times and scores — at least 3 occurrences)
    - `src/routes/+page.svelte` does NOT contain `scroll-mt-14`
    - `src/routes/+page.svelte` does NOT contain `scroll-mt-20`
    - `src/routes/+page.svelte` does NOT contain `shadow-md`
    - `src/routes/+page.svelte` does NOT contain `shadow-lg`
    - `src/routes/+page.svelte` does NOT contain `bg-gradient-to-r from-blue-50 to-indigo-50`
    - Event name `<h3>` no longer uses `text-sm` as its primary class — uses `text-base font-semibold` instead
    - Script block (registerTodaySection action, $derived blocks, Splash mount, onMount handler) is functionally unchanged
    - `yarn check` exits 0
  </acceptance_criteria>
  <done>Schedule page has sticky neutral-50 date headers at top-10, no shadows or gradients, monospace times/scores, larger event-name baseline, and the project type-checks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Shooters page — neutral palette, monospace, Norwegian copy</name>
  <files>src/routes/skyttere/+page.svelte</files>
  <read_first>
    - src/routes/skyttere/+page.svelte (full file — note `<style>` block at lines 370-394 must be preserved verbatim)
    - .planning/phases/03-ux-redesign/03-UI-SPEC.md (Copywriting Contract for Norwegian replacements; Color "Colors to remove" table)
    - .planning/phases/03-ux-redesign/03-PATTERNS.md (skyttere/+page.svelte section)
  </read_first>
  <action>
    Edit `src/routes/skyttere/+page.svelte`. Keep the `<script lang="ts">` block (lines 1-14) and the `<style>` block (lines 370-394) unchanged.

    Card and header palette cleanup:
    - `<details>` wrapper (line 65): remove `shadow-md` and `sm:shadow-lg`, change `border-gray-200` to `border-neutral-200`. Keep `overflow-hidden rounded-lg`, `sm:rounded-xl`, the `[&_summary::-webkit-details-marker]:hidden`, and `[&_summary::marker]:hidden` selectors.
    - `<summary>` (line 69): replace `bg-gradient-to-r from-blue-50 to-indigo-50 px-3 py-3 transition-colors hover:from-blue-100 hover:to-indigo-100 sm:px-6 sm:py-4` with `bg-neutral-100 px-3 py-3 sm:px-6 sm:py-4`. Remove all `border-gray-200` in favour of `border-neutral-200`.

    Typography:
    - Shooter `<h2>` (line 75): keep `text-lg font-semibold text-gray-900 sm:text-xl` (already valid for heading role per UI-SPEC).
    - Add `font-mono` to time `<span>` elements that render `formatNorwegianTime(...)`. Specifically: line 95-97 next-event time span, line 130 desktop upcoming-events time span, line 175 mobile event-time span, line 213-215 desktop oppropstid time span, line 294-296 desktop event oppropstid time span.
    - Add `font-mono` to score `<div>` and `<span>` elements rendering `series.sum` or `lastSeries.sum`: line 187 mobile final score `<div class="text-sm font-bold text-blue-600">`, line 237 mobile series total `<span class="font-bold text-blue-600">`, line 318-320 desktop series total `<span class="text-lg font-bold text-blue-600">`.

    Norwegian copy replacements (verbatim — keep punctuation):
    - Line 22 loading copy: `Loading shooters data...` → `Laster inn skyttere...`
    - Line 26 error heading: `Error loading data:` → `Feil ved lasting av data:`
    - Line 27 error body: `Error: {error}` → `Feil: {error}` (preserve the `{error}` Svelte expression)
    - Line 349 desktop empty: `No results yet` → `Ingen resultater enda`
    - Line 360 empty events: `No events scheduled` → `Ingen skytinger planlagt`

    DO NOT add `pb-16` to the outer container — Plan 01's layout wrapper provides it. DO NOT modify the `<style>` block.
  </action>
  <verify>
    <automated>grep -q 'Laster inn skyttere' src/routes/skyttere/+page.svelte && grep -q 'Feil ved lasting av data:' src/routes/skyttere/+page.svelte && grep -q 'Feil: {error}' src/routes/skyttere/+page.svelte && grep -q 'Ingen resultater enda' src/routes/skyttere/+page.svelte && grep -q 'Ingen skytinger planlagt' src/routes/skyttere/+page.svelte && grep -q 'bg-neutral-100' src/routes/skyttere/+page.svelte && grep -q 'font-mono' src/routes/skyttere/+page.svelte && ! grep -q 'Loading shooters data' src/routes/skyttere/+page.svelte && ! grep -q 'Error loading data' src/routes/skyttere/+page.svelte && ! grep -q 'No results yet' src/routes/skyttere/+page.svelte && ! grep -q 'No events scheduled' src/routes/skyttere/+page.svelte && ! grep -q 'shadow-md' src/routes/skyttere/+page.svelte && ! grep -q 'shadow-lg' src/routes/skyttere/+page.svelte && ! grep -q 'bg-gradient-to-r from-blue-50 to-indigo-50' src/routes/skyttere/+page.svelte && grep -q 'details\[open\] summary .arrow' src/routes/skyttere/+page.svelte && yarn check</automated>
  </verify>
  <acceptance_criteria>
    - `src/routes/skyttere/+page.svelte` contains Norwegian strings: `Laster inn skyttere...`, `Feil ved lasting av data:`, `Feil: {error}`, `Ingen resultater enda`, `Ingen skytinger planlagt`
    - `src/routes/skyttere/+page.svelte` contains class `bg-neutral-100` on the `<summary>` element (replacing the gradient)
    - `src/routes/skyttere/+page.svelte` contains `font-mono` (used on times and scores — at least 4 occurrences)
    - `src/routes/skyttere/+page.svelte` does NOT contain `Loading shooters data`, `Error loading data`, `Error: {error}`, `No results yet`, `No events scheduled`
    - `src/routes/skyttere/+page.svelte` does NOT contain `shadow-md`, `shadow-lg`, or `bg-gradient-to-r from-blue-50 to-indigo-50`
    - `<style>` block still present and includes `details[open] summary .arrow` rotation rule
    - Script block (lines 1-14) is unchanged
    - `yarn check` exits 0
  </acceptance_criteria>
  <done>Shooters page uses neutral palette, monospace numerals, fully Norwegian copy, and the project type-checks with the existing `<style>` block intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Premieliste page — remove shadow + emoji, neutral palette</name>
  <files>src/routes/premieliste/+page.svelte</files>
  <read_first>
    - src/routes/premieliste/+page.svelte (full file — note all 5 emoji locations: lines 99, 132, 155, 183, 211)
    - .planning/phases/03-ux-redesign/03-UI-SPEC.md (Color "Colors to remove" table — `premieliste/+page.svelte:110 shadow-sm`)
    - .planning/phases/03-ux-redesign/03-PATTERNS.md (premieliste/+page.svelte section)
  </read_first>
  <action>
    Edit `src/routes/premieliste/+page.svelte`. Keep all script logic (lines 1-82) unchanged.

    Card shadow cleanup:
    - Shooter card `<div>` (line 110): remove `shadow-sm` from `rounded-lg border border-gray-200 bg-white p-4 shadow-sm` so the class becomes `rounded-lg border border-gray-200 bg-white p-4`.

    Emoji removal (per D-03 and D-10 — no emojis anywhere in the new design):
    - Line 99 empty-state target emoji `<div class="mb-4 text-4xl">🎯</div>`: delete this entire `<div>` element.
    - Line 132 distinction-pill prefix: change `🏆 {distinction.name}` to `{distinction.name}` (remove the `🏆 ` prefix, keep the surrounding `<span>` and the optional `(eventType)` suffix).
    - Line 155 Gavepremier section header: delete the wrapping `<span class="text-2xl">🎁</span>` element.
    - Line 183 Begere section header: delete the wrapping `<span class="text-2xl">🏆</span>` element.
    - Line 211 Medaljer section header: delete the wrapping `<span class="text-2xl">🥇</span>` element.

    DO NOT add `pb-16` to the outer container — Plan 01's layout wrapper provides it. DO NOT translate the existing Norwegian copy. DO NOT touch the prize-summary `$derived` logic, the `categorizePrize` function, or the totals section.
  </action>
  <verify>
    <automated>! grep -q 'shadow-sm' src/routes/premieliste/+page.svelte && ! grep -q '🎯' src/routes/premieliste/+page.svelte && ! grep -q '🎁' src/routes/premieliste/+page.svelte && ! grep -q '🥇' src/routes/premieliste/+page.svelte && ! grep -q '🏆' src/routes/premieliste/+page.svelte && grep -q 'rounded-lg border border-gray-200 bg-white p-4' src/routes/premieliste/+page.svelte && grep -q 'Gavepremier' src/routes/premieliste/+page.svelte && grep -q 'Begere' src/routes/premieliste/+page.svelte && grep -q 'Medaljer' src/routes/premieliste/+page.svelte && yarn check && yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `src/routes/premieliste/+page.svelte` does NOT contain `shadow-sm`
    - `src/routes/premieliste/+page.svelte` does NOT contain emoji `🎯`, `🎁`, `🥇`, or `🏆`
    - `src/routes/premieliste/+page.svelte` contains shooter card class `rounded-lg border border-gray-200 bg-white p-4`
    - Norwegian section headings `Gavepremier`, `Begere`, `Medaljer` remain present
    - Script block (lines 1-82) is functionally unchanged: `getEventTypeName`, `categorizePrize`, and `prizeSummary` $derived block intact
    - `yarn check` exits 0
    - `yarn build` exits 0 (final wave gate — proves all three plans compose cleanly)
  </acceptance_criteria>
  <done>Premieliste page has no shadows and no emoji, retains Norwegian summary structure, and a full production build passes.</done>
</task>

</tasks>

<verification>
- `yarn check` after each task — zero TypeScript or Svelte errors
- `yarn build` at end of Task 3 — final wave production-build gate
- All four ROADMAP success criteria visually verifiable after this plan ships (executed by execute-phase, not by this plan):
  1. Bottom tab bar visible on all three routes (Plan 01)
  2. No gradients/shadows anywhere; monospace times/scores; `text-base font-semibold` data (this plan)
  3. Solid-fill Norwegian badges (Plan 02)
  4. Sticky date headers with "I dag" + auto-scroll (this plan + existing `getDateLabel`/`registerTodaySection`)
</verification>

<success_criteria>
1. Schedule page date section headers are CSS-sticky at `top-10`, neutral background, no shadows, no gradients
2. Schedule page event names use `text-base font-semibold`; times and scores use `font-mono`
3. Shooters page has no shadows, no gradients, monospace times/scores, fully Norwegian copy, `<style>` block preserved
4. Premieliste page shooter card has no shadow; all five emoji locations cleared
5. `yarn check` and `yarn build` pass end-of-plan
</success_criteria>

<output>
Create `.planning/phases/03-ux-redesign/03-03-SUMMARY.md` when done.
</output>
