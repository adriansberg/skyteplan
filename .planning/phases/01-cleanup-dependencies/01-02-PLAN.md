---
phase: 01-cleanup-dependencies
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - package.json
  - yarn.lock
autonomous: false
requirements:
  - DEPS-01
user_setup: []

must_haves:
  truths:
    - "All patch/minor packages are at the exact target versions on disk"
    - "svelte is at 5.55.7 — required prerequisite for @sveltejs/vite-plugin-svelte@7 in Wave 4"
    - "yarn check and yarn build both pass with zero errors after the bump batch"
    - "No remaining peer-dep warnings related to svelte version"
  artifacts:
    - path: "package.json"
      provides: "Pinned target versions for svelte, svelte-check, eslint, prettier, graphql-request, @vercel/analytics"
      contains: "svelte"
    - path: "yarn.lock"
      provides: "Resolved tree consistent with package.json"
  key_links:
    - from: "package.json devDependencies"
      to: "node_modules/svelte/package.json"
      via: "yarn add"
      pattern: "\"svelte\": \"5\\.55\\.7\""
---

<objective>
Apply the first batch of dependency bumps: patch and minor versions only, no major. Critically this includes bumping svelte from 5.37.1 to 5.55.7, which is the peer prerequisite for @sveltejs/vite-plugin-svelte@7 in Wave 4. This plan changes only package.json and yarn.lock — no source edits.

Purpose: Land safe bumps in one pass so Waves 3–5 isolate one major upgrade per wave. svelte must reach ≥5.46.4 before vite-plugin-svelte@7 is installed; doing it here removes that constraint as a downstream blocker.

Output: Updated package.json + yarn.lock with the six target versions resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-cleanup-dependencies/01-RESEARCH.md
@.planning/phases/01-cleanup-dependencies/01-PATTERNS.md
@.planning/phases/01-cleanup-dependencies/01-01-SUMMARY.md
@package.json
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification before install</name>
  <what-built>About to install six package versions: svelte@5.55.7, svelte-check@4.4.8, eslint@9.39.4, prettier@3.8.3, graphql-request@7.4.0, @vercel/analytics@1.6.1. All are flagged [ASSUMED] in the RESEARCH.md Package Legitimacy Audit because slopcheck was unavailable during research.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/svelte and confirm the package is published by the @sveltejs organization with a recent release in the 5.5x range
    2. Visit https://www.npmjs.com/package/svelte-check and confirm @sveltejs publisher with 4.4.x latest
    3. Visit https://www.npmjs.com/package/eslint and confirm OpenJS Foundation publisher with 9.39.x latest
    4. Visit https://www.npmjs.com/package/prettier and confirm prettier-bot/prettier publisher with 3.8.x latest
    5. Visit https://www.npmjs.com/package/graphql-request and confirm @jasonkuhrt publisher with 7.4.x latest
    6. Visit https://www.npmjs.com/package/@vercel/analytics and confirm @vercel publisher with 1.6.x latest
    7. Confirm none of these look like typosquatted clones (e.g., "svelt", "svelte-cheker")
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `yarn add` commands, or describe specific concerns.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply patch/minor bumps via yarn add</name>
  <files>package.json, yarn.lock</files>
  <read_first>
    - package.json (current versions — svelte ^5.0.0, svelte-check ^4.0.0, eslint ^9.18.0, prettier ^3.4.2, @vercel/analytics ^1.5.0, graphql-request ^7.2.0)
    - .planning/phases/01-cleanup-dependencies/01-RESEARCH.md (Installation Step 1 commands and exact target versions)
    - .planning/phases/01-cleanup-dependencies/01-PATTERNS.md (Package management note: use yarn add, not hand-edit, to keep lockfile consistent)
  </read_first>
  <action>
    Run two yarn commands in sequence from the project root. First: `yarn add -D svelte@5.55.7 svelte-check@4.4.8 eslint@9.39.4 prettier@3.8.3`. Then: `yarn add graphql-request@7.4.0 @vercel/analytics@1.6.1`. Do not edit package.json by hand — yarn writes the pinned versions and regenerates yarn.lock atomically. Do not bump @sveltejs/kit, vite, vite-plugin-svelte, adapter-vercel, or typescript in this task — those are reserved for Waves 3–5. After both commands complete, run `yarn check` and `yarn build` to confirm the stack still builds with the bumped versions. If yarn produces a peer-dep warning naming svelte@<5.46.4, halt and report — the bump did not land.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const d={...p.devDependencies,...p.dependencies}; const t={svelte:'5.55.7','svelte-check':'4.4.8',eslint:'9.39.4',prettier:'3.8.3','graphql-request':'7.4.0','@vercel/analytics':'1.6.1'}; for (const [k,v] of Object.entries(t)) { const got=(d[k]||'').replace(/^[\\^~]/,''); if (got !== v) { console.error('FAIL',k,'expected',v,'got',d[k]); process.exit(1); } } console.log('OK');" && yarn check && yarn build</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies.svelte resolves to exactly `5.55.7` (with or without ^/~ prefix; pin-equivalent after yarn add)
    - package.json devDependencies["svelte-check"] resolves to `4.4.8`
    - package.json devDependencies.eslint resolves to `9.39.4`
    - package.json devDependencies.prettier resolves to `3.8.3`
    - package.json dependencies["graphql-request"] resolves to `7.4.0`
    - package.json dependencies["@vercel/analytics"] resolves to `1.6.1`
    - yarn.lock is modified (mtime newer than start of task)
    - `yarn check` exits 0
    - `yarn build` exits 0
    - `yarn install` (if re-run) reports no peer-dep warnings about svelte<5.46.4
  </acceptance_criteria>
  <done>DEPS-01 implemented: six bumps applied, lockfile regenerated, build green; svelte ≥5.46.4 unlocks Wave 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry ↔ local install | yarn fetches tarballs from registry.yarnpkg.com / npm registry mirror |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | yarn add (six packages, all [ASSUMED] in audit) | mitigate | Blocking human-verify checkpoint (Task 1) confirms publisher identity on npmjs.com before install. All six are from established orgs (@sveltejs, OpenJS, Vercel, jasonkuhrt) with multi-year histories |
| T-01-02 | Tampering | yarn.lock | accept | Lockfile regenerated by yarn from registry-verified tarballs; integrity hashes pinned automatically |
</threat_model>

<verification>
1. `yarn check` exits 0
2. `yarn build` exits 0
3. `yarn list --depth=0 2>/dev/null | grep -E "svelte@5\.55\.7|svelte-check@4\.4\.8|eslint@9\.39\.4|prettier@3\.8\.3|graphql-request@7\.4\.0|@vercel/analytics@1\.6\.1"` returns six matching lines
4. No yarn peer-dep warnings about svelte version
</verification>

<success_criteria>
- DEPS-01 satisfied — six target versions installed
- Stack still on SvelteKit 2.22 + Vite 7 + TS 5 (no major changes)
- svelte at 5.55.7 — peer prerequisite for Wave 4 satisfied in advance
</success_criteria>

<output>
Create `.planning/phases/01-cleanup-dependencies/01-02-SUMMARY.md` when done.
</output>
