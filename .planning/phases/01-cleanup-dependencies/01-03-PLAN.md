---
phase: 01-cleanup-dependencies
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - package.json
  - yarn.lock
autonomous: false
requirements:
  - DEPS-02
user_setup: []

must_haves:
  truths:
    - "@sveltejs/kit is at 2.59.1 on disk"
    - "yarn check passes after the SvelteKit bump"
    - "yarn build passes after the SvelteKit bump"
    - "All three routes (/, /skyttere, /premieliste) still type-check against the new kit types"
  artifacts:
    - path: "package.json"
      provides: "@sveltejs/kit pinned to 2.59.1"
      contains: "@sveltejs/kit"
    - path: "yarn.lock"
      provides: "Resolved tree with kit 2.59.1"
  key_links:
    - from: "package.json devDependencies"
      to: "node_modules/@sveltejs/kit/package.json"
      via: "yarn add"
      pattern: "\"@sveltejs/kit\": \"2\\.59\\.1\""
---

<objective>
Bump @sveltejs/kit from 2.22→2.59.1. Per RESEARCH.md this range has zero breaking changes for standard load-function apps. This plan exists as its own wave specifically so any post-bump type or build failure is unambiguously attributable to the kit upgrade, not bundled with the Vite/adapter major bump that follows in Wave 4.

Purpose: Prerequisite for Vite 8 + adapter-vercel 6 + vite-plugin-svelte 7 in Wave 4. Isolating the kit bump preserves bisectability if any regression surfaces.

Output: package.json + yarn.lock updated; type-check and build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-cleanup-dependencies/01-RESEARCH.md
@.planning/phases/01-cleanup-dependencies/01-PATTERNS.md
@.planning/phases/01-cleanup-dependencies/01-02-SUMMARY.md
@package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify @sveltejs/kit@2.59.1 on npm</name>
  <what-built>About to install @sveltejs/kit@2.59.1. Flagged [ASSUMED] in RESEARCH.md Package Legitimacy Audit because slopcheck was unavailable.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/@sveltejs/kit
    2. Confirm publisher is the @sveltejs organization
    3. Confirm 2.59.1 is a real published version (or that 2.59.x line exists with no clear typosquat)
    4. Confirm the repository link points to github.com/sveltejs/kit
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the kit upgrade, or describe specific concerns.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bump @sveltejs/kit to 2.59.1</name>
  <files>package.json, yarn.lock</files>
  <read_first>
    - package.json (current @sveltejs/kit version after Plan 01-02; still ^2.22.0 pre-Wave-3)
    - .planning/phases/01-cleanup-dependencies/01-RESEARCH.md (Installation Step 2; Assumption A1: 2.22→2.59 has no breaking changes for standard apps)
    - svelte.config.js (no changes expected; adapter() call must remain compatible)
    - src/routes/+page.ts (current import `import type { Load } from '@sveltejs/kit'` — must still resolve after bump)
    - src/routes/skyttere/+page.ts (same Load type)
    - src/routes/premieliste/+page.ts (PageLoad from `./$types` — generated by sveltekit sync, must still emit)
  </read_first>
  <action>
    Run `yarn add -D @sveltejs/kit@2.59.1` from the project root. Do not edit package.json by hand. After yarn finishes, run `yarn check` and `yarn build` in sequence. If `yarn check` fails with a kit-related type error (e.g., Load signature change), capture the exact error and stop — the assumption A1 from RESEARCH.md is wrong and the planner must be re-engaged. If both pass, the bump is clean. Do not touch any other dependency in this task.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const got=(p.devDependencies['@sveltejs/kit']||'').replace(/^[\\^~]/,''); if (got !== '2.59.1') { console.error('FAIL got',p.devDependencies['@sveltejs/kit']); process.exit(1); } console.log('OK');" && yarn check && yarn build</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies["@sveltejs/kit"] resolves to `2.59.1`
    - yarn.lock entry for @sveltejs/kit names version 2.59.1
    - `yarn check` exits 0
    - `yarn build` exits 0
    - No `vite` version change introduced by this task (still 7.x)
    - No `@sveltejs/vite-plugin-svelte` change introduced by this task (still 6.x)
    - No `@sveltejs/adapter-vercel` change introduced by this task (still 5.x)
  </acceptance_criteria>
  <done>DEPS-02 implemented: kit at 2.59.1, build green, ready for Wave 4 Vite major.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry ↔ local install | yarn fetches kit tarball from registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | yarn add @sveltejs/kit@2.59.1 ([ASSUMED]) | mitigate | Blocking human-verify checkpoint confirms @sveltejs publisher and version on npmjs.com before install |
</threat_model>

<verification>
1. `yarn check` exits 0
2. `yarn build` exits 0
3. `yarn list --depth=0 2>/dev/null | grep "@sveltejs/kit@2.59"` returns a match
4. Vite still on 7.x (verified by `yarn list --depth=0 2>/dev/null | grep "^├─ vite@7"` or equivalent)
</verification>

<success_criteria>
- DEPS-02 satisfied — kit at 2.59.1
- Vite/adapter/typescript unchanged from Plan 02 state
- Type check and build green
</success_criteria>

<output>
Create `.planning/phases/01-cleanup-dependencies/01-03-SUMMARY.md` when done.
</output>
