---
phase: 01-cleanup-dependencies
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - package.json
  - yarn.lock
autonomous: false
requirements:
  - DEPS-03
user_setup: []

must_haves:
  truths:
    - "vite is at 8.0.13 on disk"
    - "@sveltejs/vite-plugin-svelte is at 7.1.2 on disk"
    - "@sveltejs/adapter-vercel is at 6.3.3 on disk"
    - "yarn check passes on the Rolldown-era Vite stack"
    - "yarn build passes and produces a Vercel-compatible build output"
    - "No duplicate vite-plugin-svelte-inspector warnings during build"
  artifacts:
    - path: "package.json"
      provides: "vite 8.0.13, vite-plugin-svelte 7.1.2, adapter-vercel 6.3.3 pinned"
    - path: "yarn.lock"
      provides: "Resolved tree with the three majors applied together"
    - path: "vite.config.ts"
      provides: "Unchanged — uses only plugins: [tailwindcss(), sveltekit()], compatible with Vite 8"
    - path: "svelte.config.js"
      provides: "Unchanged — adapter() and vitePreprocess() compatible with new versions"
  key_links:
    - from: "package.json devDependencies"
      to: "node_modules/vite/package.json"
      via: "yarn add"
      pattern: "\"vite\": \"8\\.0\\.13\""
    - from: "@sveltejs/vite-plugin-svelte@7"
      to: "svelte@5.55.7 (already installed in Plan 02)"
      via: "peer dependency"
      pattern: "peerDep svelte >=5.46.4"
---

<objective>
Apply the three majors that must move together: Vite 7→8, @sveltejs/vite-plugin-svelte 6→7, @sveltejs/adapter-vercel 5→6. RESEARCH.md confirms vite.config.ts and svelte.config.js need no edits because this project uses only `plugins: [tailwindcss(), sveltekit()]` with no `build.rollupOptions` or `build.esbuildOptions`. The svelte ≥5.46.4 peer constraint required by vite-plugin-svelte@7 was already satisfied in Plan 02.

Purpose: Land the Vite 8 / Rolldown-era stack while still on TypeScript 5, isolating any TS6-related failures to Wave 5. vite-plugin-svelte@7 also integrates the inspector that was previously a separate package — RESEARCH.md notes no manual config needed.

Output: package.json + yarn.lock updated; type-check and build pass; deployment artifact compatible with adapter-vercel 6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-cleanup-dependencies/01-RESEARCH.md
@.planning/phases/01-cleanup-dependencies/01-PATTERNS.md
@.planning/phases/01-cleanup-dependencies/01-03-SUMMARY.md
@package.json
@vite.config.ts
@svelte.config.js
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify vite@8.0.13, vite-plugin-svelte@7.1.2, adapter-vercel@6.3.3 on npm</name>
  <what-built>About to install three majors: vite@8.0.13, @sveltejs/vite-plugin-svelte@7.1.2, @sveltejs/adapter-vercel@6.3.3. All [ASSUMED] in RESEARCH.md Package Legitimacy Audit because slopcheck was unavailable.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/vite and confirm @vitejs publisher with 8.0.x line published
    2. Visit https://www.npmjs.com/package/@sveltejs/vite-plugin-svelte and confirm @sveltejs publisher with 7.1.x line
    3. Visit https://www.npmjs.com/package/@sveltejs/adapter-vercel and confirm @sveltejs publisher with 6.3.x line
    4. On the @sveltejs/vite-plugin-svelte page, confirm in the "peer dependencies" section that svelte is required at >=5.46 (matches svelte 5.55.7 already installed)
    5. Confirm none look like typosquatted clones
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the three-major install, or describe specific concerns.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install Vite 8, vite-plugin-svelte 7, adapter-vercel 6 in a single yarn add</name>
  <files>package.json, yarn.lock</files>
  <read_first>
    - package.json (current vite ^7.0.4, @sveltejs/vite-plugin-svelte ^6.0.0, @sveltejs/adapter-vercel ^5.6.3 after Plans 02–03)
    - vite.config.ts (current content: defineConfig({ plugins: [tailwindcss(), sveltekit()] }) — must not require edits per RESEARCH.md)
    - svelte.config.js (current content: vitePreprocess + adapter() — must not require edits per RESEARCH.md)
    - .planning/phases/01-cleanup-dependencies/01-RESEARCH.md (Installation Step 3; Pitfalls 1, 3; State of the Art section on Rolldown)
    - .planning/phases/01-cleanup-dependencies/01-PATTERNS.md (no vite.config.ts or svelte.config.js changes needed)
  </read_first>
  <action>
    Run `yarn add -D vite@8.0.13 @sveltejs/vite-plugin-svelte@7.1.2 @sveltejs/adapter-vercel@6.3.3` from the project root as a single command so the resolver lands the three majors together with one peer-dep pass. Do not edit vite.config.ts. Do not edit svelte.config.js. After the install, run `yarn check` and then `yarn build`. During the build, watch stderr for any "duplicate vite-plugin-svelte-inspector" or "vite-plugin-svelte-inspector ignored" warnings (Pitfall 3 from RESEARCH.md) and for any peer-dep warnings about svelte being below 5.46.4 (Pitfall 1). If either warning class appears, halt and report — do not silently proceed. If both `yarn check` and `yarn build` exit 0 with no warnings, the wave is clean.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const d=p.devDependencies; const t={vite:'8.0.13','@sveltejs/vite-plugin-svelte':'7.1.2','@sveltejs/adapter-vercel':'6.3.3'}; for (const [k,v] of Object.entries(t)) { const got=(d[k]||'').replace(/^[\\^~]/,''); if (got !== v) { console.error('FAIL',k,'expected',v,'got',d[k]); process.exit(1); } } console.log('OK');" && yarn check && yarn build 2>&1 | tee /tmp/build.log && ! grep -E "duplicate plugin|inspector ignored|Unmet peer dependency" /tmp/build.log</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies.vite resolves to `8.0.13`
    - package.json devDependencies["@sveltejs/vite-plugin-svelte"] resolves to `7.1.2`
    - package.json devDependencies["@sveltejs/adapter-vercel"] resolves to `6.3.3`
    - yarn.lock entries for all three packages name the target versions
    - vite.config.ts is byte-identical to its pre-task content (no edits required)
    - svelte.config.js is byte-identical to its pre-task content (no edits required)
    - `yarn check` exits 0
    - `yarn build` exits 0
    - Build stderr/stdout contains no "duplicate plugin" warnings
    - Build stderr/stdout contains no "Unmet peer dependency" warnings naming svelte
    - typescript still at 5.x (not yet bumped — that is Wave 5)
  </acceptance_criteria>
  <done>DEPS-03 implemented: Vite 8 + plugin-svelte 7 + adapter-vercel 6 landed together; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry ↔ local install | yarn fetches three new majors from registry |
| Vercel build pipeline ↔ adapter-vercel output | Adapter major bump changes output format; Vercel must accept it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | yarn add for three [ASSUMED] majors | mitigate | Blocking human-verify checkpoint confirms publisher and peer constraints on npmjs.com before install |
| T-01-03 | Denial of Service | Vercel deploy may reject new adapter output | accept | Open Question Q2 in RESEARCH.md flags this as LOW risk; deployment verification deferred to manual post-merge step (RESEARCH.md says no code changes visible) |
</threat_model>

<verification>
1. `yarn check` exits 0
2. `yarn build` exits 0
3. `yarn list --depth=0 2>/dev/null | grep -E "vite@8\.0\.13|@sveltejs/vite-plugin-svelte@7\.1\.2|@sveltejs/adapter-vercel@6\.3\.3"` returns three matches
4. No duplicate-plugin warnings in build output
5. No svelte peer-dep warnings
</verification>

<success_criteria>
- DEPS-03 satisfied — three majors at target
- vite.config.ts and svelte.config.js unchanged (zero source edits in this wave)
- TypeScript still at 5.x (isolated for Wave 5)
- Build output is a Vercel-compatible artifact
</success_criteria>

<output>
Create `.planning/phases/01-cleanup-dependencies/01-04-SUMMARY.md` when done.
</output>
