---
phase: 04-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.html
  - static/manifest.json
  - src/app.css
  - src/routes/+layout.svelte
  - src/routes/+page.svelte
autonomous: true
requirements:
  - POL-02

must_haves:
  truths:
    - "iOS PWA status bar is transparent (black-translucent) with no white gap at the top"
    - "Top bar content is not obscured by iOS status bar overlay"
    - "Sticky date headers track the dynamic top bar height on all devices"
    - "Theme color is neutral-50 (#fafafa) in both app.html and manifest.json"
  artifacts:
    - path: "src/app.html"
      provides: "viewport-fit=cover + black-translucent + #fafafa theme-color"
      contains: "viewport-fit=cover"
    - path: "static/manifest.json"
      provides: "theme_color matching app.html"
      contains: "#fafafa"
    - path: "src/app.css"
      provides: "--top-bar-height CSS variable"
      contains: "--top-bar-height"
    - path: "src/routes/+layout.svelte"
      provides: "safe-area-inset-top padding on top bar"
      contains: "env(safe-area-inset-top)"
    - path: "src/routes/+page.svelte"
      provides: "sticky date headers using --top-bar-height"
      contains: "var(--top-bar-height)"
  key_links:
    - from: "src/app.css :root"
      to: "src/routes/+page.svelte sticky header"
      via: "CSS variable --top-bar-height"
      pattern: "var\\(--top-bar-height\\)"
    - from: "src/routes/+layout.svelte header"
      to: "env(safe-area-inset-top)"
      via: "inline style attribute"
      pattern: "env\\(safe-area-inset-top\\)"
---

<objective>
Configure iOS PWA meta tags for full-bleed display and establish the top bar safe-area infrastructure that sticky headers depend on.

Purpose: With black-translucent status bar the iOS status bar overlays app content — the top bar must compensate with env(safe-area-inset-top) padding, and sticky date headers must track the resulting dynamic height.
Output: Updated app.html, manifest.json, app.css, +layout.svelte, +page.svelte — no new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-polish/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: iOS meta tags + manifest theme-color</name>
  <files>src/app.html, static/manifest.json</files>
  <read_first>
    - src/app.html — current viewport meta (line 9), status-bar meta (line 16), theme-color meta (line 20)
    - static/manifest.json — current theme_color value (line 8)
  </read_first>
  <action>
    In src/app.html make three targeted edits (per D-05):
    1. viewport meta (line 9): append `, viewport-fit=cover` to the content attribute value so it reads `width=device-width, initial-scale=1, viewport-fit=cover`
    2. apple-mobile-web-app-status-bar-style (line 16): change content from `default` to `black-translucent`
    3. theme-color meta (line 20): change content from `#1f2937` to `#fafafa`
    Leave all other meta tags (apple-mobile-web-app-capable, apple-mobile-web-app-title, msapplication-TileColor) unchanged.

    In static/manifest.json: change `theme_color` from `#1f2937` to `#fafafa`. All other fields unchanged.
  </action>
  <verify>
    <automated>grep -c 'viewport-fit=cover' /Users/asb/Projects/stordalen/src/app.html && grep -c 'black-translucent' /Users/asb/Projects/stordalen/src/app.html && grep -c '"#fafafa"' /Users/asb/Projects/stordalen/src/app.html && grep -c '"#fafafa"' /Users/asb/Projects/stordalen/static/manifest.json</automated>
  </verify>
  <done>app.html contains viewport-fit=cover, black-translucent, #fafafa; manifest.json theme_color is #fafafa</done>
</task>

<task type="auto">
  <name>Task 2: CSS variable + top bar safe-area + sticky header fix</name>
  <files>src/app.css, src/routes/+layout.svelte, src/routes/+page.svelte</files>
  <read_first>
    - src/app.css — full file (2 lines currently)
    - src/routes/+layout.svelte — header element at lines 22-27; current class `h-10` on the header
    - src/routes/+page.svelte — sticky date header div at line 111 (class contains `sticky top-10`); section wrapper div at line 107 (class contains `scroll-mt-10`)
  </read_first>
  <action>
    In src/app.css: append a `:root` block after the two existing lines (per D-07). Use tab indentation matching project convention. The block is:
      :root { --top-bar-height: calc(2.5rem + env(safe-area-inset-top)); }
    Use a tab before the property, and a newline between the @plugin line and the :root block.

    In src/routes/+layout.svelte: on the header element (lines 22-24), make two changes (per D-06):
    1. Remove `h-10` from the class string
    2. Add a style attribute: `style="height: calc(2.5rem + env(safe-area-inset-top)); padding-top: env(safe-area-inset-top)"`
    All other class tokens (sticky, top-0, z-40, flex, items-center, justify-between, border-b, border-neutral-200, bg-neutral-50, px-4) remain unchanged.

    In src/routes/+page.svelte make two targeted edits (per D-07):
    1. Sticky date header div (line 111): remove `top-10` from its class string; add `style="top: var(--top-bar-height)"` attribute
    2. Section wrapper div (line 107, class contains `scroll-mt-10`): remove `scroll-mt-10` from its class; add `style="scroll-margin-top: var(--top-bar-height)"` attribute
  </action>
  <verify>
    <automated>grep -c '\-\-top-bar-height' /Users/asb/Projects/stordalen/src/app.css && grep -c 'env(safe-area-inset-top)' /Users/asb/Projects/stordalen/src/routes/+layout.svelte && grep -c 'var(--top-bar-height)' /Users/asb/Projects/stordalen/src/routes/+page.svelte && cd /Users/asb/Projects/stordalen && yarn check 2>&1 | tail -5</automated>
  </verify>
  <done>--top-bar-height defined in app.css :root; layout header uses env(safe-area-inset-top); +page.svelte sticky header and section wrapper use var(--top-bar-height); yarn check passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static HTML → browser | app.html meta tags consumed by browser; no user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |

No security-relevant changes: meta tag edits and CSS are purely presentational with no auth, data, or input surfaces.
</threat_model>

<verification>
After both tasks complete:
- `yarn check` exits 0
- `grep 'viewport-fit=cover' src/app.html` matches
- `grep 'black-translucent' src/app.html` matches
- `grep '#fafafa' src/app.html` matches
- `grep '#fafafa' static/manifest.json` matches
- `grep '\-\-top-bar-height' src/app.css` matches
- `grep 'env(safe-area-inset-top)' src/routes/+layout.svelte` matches
- `grep 'var(--top-bar-height)' src/routes/+page.svelte` matches (2 occurrences — sticky header + scroll wrapper)
- `grep 'top-10' src/routes/+page.svelte` returns no matches (removed)
- `grep 'h-10' src/routes/+layout.svelte` returns no matches (removed)
</verification>

<success_criteria>
- iOS PWA shows no white gap at status bar; content starts below status bar on iPhone
- Sticky date headers remain below the top bar on both iOS (with safe area) and non-iOS (safe area = 0)
- theme-color matches neutral-50 (#fafafa) on Android Chrome address bar
- yarn check passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/04-polish/04-01-SUMMARY.md` when done
</output>
