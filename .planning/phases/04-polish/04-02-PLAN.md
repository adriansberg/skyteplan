---
phase: 04-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/routes/+page.svelte
  - src/routes/skyttere/+page.svelte
  - src/routes/premieliste/+page.svelte
autonomous: true
requirements:
  - POL-01

must_haves:
  truths:
    - "Switching between tabs shows pulse-bar skeleton instead of blank/stale content"
    - "Skeleton never flashes on initial page load (SSR has data, navigating is null)"
    - "Schedule skeleton: one date-header bar + 4 event row bars"
    - "Skyttere skeleton: 6 shooter name bars of varied width"
    - "Premieliste skeleton: 3 shooter blocks each with 3 prize row bars"
  artifacts:
    - path: "src/routes/+page.svelte"
      provides: "navigating import + {#if navigating} skeleton branch before error/content"
      contains: "navigating"
    - path: "src/routes/skyttere/+page.svelte"
      provides: "navigating import + widened guard condition"
      contains: "navigating"
    - path: "src/routes/premieliste/+page.svelte"
      provides: "navigating import + outermost {#if navigating} wrapper"
      contains: "navigating"
  key_links:
    - from: "$app/state"
      to: "each +page.svelte"
      via: "import { navigating }"
      pattern: "import.*navigating.*\\$app/state"
    - from: "skeleton branch"
      to: "content branch"
      via: "{#if navigating} / {:else if} / {:else}"
      pattern: "\\{#if navigating\\}"
---

<objective>
Add animated pulse-bar skeleton loading screens to all three routes, shown during client-side tab navigation.

Purpose: Eliminates blank/stale content flash when switching tabs — shooters see immediate visual feedback that data is loading.
Output: Three modified route pages. No new files, no new components (per D-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-polish/04-CONTEXT.md

<interfaces>
<!-- Key facts about navigating from $app/state — executor must not use $app/stores (deprecated) -->

navigating: Navigation | null
  - null during SSR and when idle (no navigation in flight)
  - non-null (Navigation object) during any client-side route transition
  - Import: import { navigating } from '$app/state'
  - Already used by BottomTabBar.svelte (same module, same pattern)

Skeleton element anatomy (D-01, D-02):
  Container: class="container mx-auto px-2 py-4 pt-6"
  Bar:       class="animate-pulse rounded bg-neutral-200 {height} {width}"
  Heights:   h-7 (date header), h-24 (event card), h-14 (shooter row), h-6 (name bar), h-5 (prize row)
  Widths:    w-full (full-width), w-40/w-36/w-48/w-52/w-44/w-32 (varied name bars)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schedule page skeleton (+page.svelte)</name>
  <files>src/routes/+page.svelte</files>
  <read_first>
    - src/routes/+page.svelte — full file; note the outer {#if !showSplash} at line 87, and the inner {#if error} / {:else if shooters} structure at lines 88-288
  </read_first>
  <action>
    Add `import { navigating } from '$app/state'` to the script block imports (after the existing import { onMount } from 'svelte' line).

    Inside the existing `{#if !showSplash}` block, insert a new `{#if navigating}` branch as the FIRST conditional — before the existing `{#if error}` check. The structure becomes:

      {#if !showSplash}
        {#if navigating}
          [skeleton markup — see shape below]
        {:else if error}
          [existing error block — unchanged]
        {:else if shooters}
          [existing content block — unchanged]
        {/if}
      {/if}

    Skeleton markup shape (per D-02: one date-header placeholder + 4 event row placeholders):
      Outer div: class="container mx-auto px-2 py-4 pt-6"
        Date header bar: class="mb-4 h-7 w-40 animate-pulse rounded bg-neutral-200"
        Then {#each Array(4) as _, i (i)}: each bar class="mb-3 h-24 w-full animate-pulse rounded bg-neutral-200"

    Do NOT modify the error block, the content block, or any logic in the script section. The only additions are the import statement and the skeleton branch.
  </action>
  <verify>
    <automated>grep -c 'navigating' /Users/asb/Projects/stordalen/src/routes/+page.svelte && grep -c 'animate-pulse' /Users/asb/Projects/stordalen/src/routes/+page.svelte && cd /Users/asb/Projects/stordalen && yarn check 2>&1 | tail -5</automated>
  </verify>
  <done>navigating imported and skeleton branch present; yarn check passes; existing content/error blocks unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Skyttere page skeleton (skyttere/+page.svelte)</name>
  <files>src/routes/skyttere/+page.svelte</files>
  <read_first>
    - src/routes/skyttere/+page.svelte — full file; note the {#if !shooters && !error} guard at line 20 and its inner div at lines 21-23
  </read_first>
  <action>
    Add `import { navigating } from '$app/state'` to the script block (after the existing import statements).

    Replace the existing guard condition on the first branch from `{#if !shooters && !error}` to `{#if navigating || (!shooters && !error)}`. This widens the condition to also show the skeleton during client navigation.

    Replace the inner content of that branch (currently a centered "Laster inn skyttere..." div) with the skeleton markup. Remove the old text div entirely.

    Skeleton markup shape (per D-02: 5-6 shooter name bars of varied width):
      Outer div: class="container mx-auto px-2 py-4 pt-6"
        {#each ['w-48', 'w-36', 'w-52', 'w-40', 'w-44', 'w-32'] as widthClass (widthClass)}
          div: class="mb-3 h-14 {widthClass} animate-pulse rounded bg-neutral-200"

    The {:else if error} and {:else if shooters} branches and all their content remain unchanged.
  </action>
  <verify>
    <automated>grep -c 'navigating' /Users/asb/Projects/stordalen/src/routes/skyttere/+page.svelte && grep -c 'animate-pulse' /Users/asb/Projects/stordalen/src/routes/skyttere/+page.svelte && grep -c 'Laster inn' /Users/asb/Projects/stordalen/src/routes/skyttere/+page.svelte</automated>
  </verify>
  <done>navigating imported; guard condition widened; skeleton bars present; "Laster inn" text absent (count = 0); yarn check passes</done>
</task>

<task type="auto">
  <name>Task 3: Premieliste page skeleton (premieliste/+page.svelte)</name>
  <files>src/routes/premieliste/+page.svelte</files>
  <read_first>
    - src/routes/premieliste/+page.svelte — full file; note the outermost div at line 89 (container mx-auto) which contains everything; note the {#if shootersWithDistinctions.length === 0} empty-state check at line 97
  </read_first>
  <action>
    Add `import { navigating } from '$app/state'` to the script block (after existing imports).

    Wrap the entire template (the outermost container div at line 89 through the end of the file before the closing script/style if any) in an `{#if navigating}` / `{:else}` guard:

      {#if navigating}
        [skeleton markup]
      {:else}
        [existing outermost container div and all contents — unchanged]
      {/if}

    Skeleton markup shape (per D-04: full skeleton until all data resolves; per D-02: 3-4 shooter blocks each with 2-3 prize row placeholders):
      Outer div: class="container mx-auto px-2 py-4 pt-6"
        {#each Array(3) as _, i (i)}
          Shooter block div: class="mb-4 rounded-lg border border-neutral-200 bg-white p-4"
            Shooter name bar: class="mb-3 h-6 w-40 animate-pulse rounded bg-neutral-200"
            {#each Array(3) as _, j (j)}
              Prize row bar: class="mb-2 h-5 w-full animate-pulse rounded bg-neutral-200"

    The existing `{#if shootersWithDistinctions.length === 0}` empty-state block remains inside the {:else} branch, untouched.
    Do NOT change shootersWithDistinctions derivation, prizeSummary derivation, or any script logic.
  </action>
  <verify>
    <automated>grep -c 'navigating' /Users/asb/Projects/stordalen/src/routes/premieliste/+page.svelte && grep -c 'animate-pulse' /Users/asb/Projects/stordalen/src/routes/premieliste/+page.svelte && cd /Users/asb/Projects/stordalen && yarn check 2>&1 | tail -5</automated>
  </verify>
  <done>navigating imported; {#if navigating} outermost guard present; 3 skeleton shooter blocks with 3 prize rows each present; existing content untouched inside {:else}; yarn check passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser state → template | navigating is a framework-internal reactive value; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |

No security-relevant changes: skeleton screens are purely presentational UI markup with no data flow, auth, or input surfaces.
</threat_model>

<verification>
After all three tasks complete:
- `grep -r "import.*navigating.*\$app/state" src/routes/` matches all three route pages
- `grep -c "animate-pulse" src/routes/+page.svelte` >= 1
- `grep -c "animate-pulse" src/routes/skyttere/+page.svelte` >= 1
- `grep -c "animate-pulse" src/routes/premieliste/+page.svelte` >= 1
- `grep "Laster inn" src/routes/skyttere/+page.svelte` returns no match
- `yarn check` exits 0
- `yarn build` exits 0
</verification>

<success_criteria>
- Switching between tabs on a mobile device shows pulse-bar animation rather than blank or stale content
- Initial hard load (SSR) does NOT show skeleton (navigating is null on SSR)
- yarn check and yarn build both pass with zero errors
</success_criteria>

<output>
Create `.planning/phases/04-polish/04-02-SUMMARY.md` when done
</output>
