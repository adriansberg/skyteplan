---
phase: 05-generic-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/manifest.json
  - src/app.html
  - vite.config.ts
  - src/lib/components/Splash.svelte
  - src/routes/+page.svelte
  - src/routes/skyttere/+page.svelte
  - src/routes/premieliste/+page.svelte
  - src/routes/+layout.svelte
  - src/routes/+error.svelte
  - static/sw.js
autonomous: true
requirements:
  - BRAND-01
tags:
  - svelte
  - sveltekit
  - pwa
  - branding

must_haves:
  truths:
    - "Browser tab title on schedule page reads 'Skyteplan - Skytterappen'"
    - "Browser tab title on shooters page reads 'Skyttere - Skytterappen'"
    - "Browser tab title on premieliste page reads 'Premieliste - Skytterappen'"
    - "PWA manifest name and short_name both read 'Skytterappen'"
    - "iOS PWA install title reads 'Skytterappen'"
    - "Top-bar logo alt text on all routes reads 'Skytterappen'"
    - "Error page visible heading text reads 'Skytterappen' (not 'Stordalen Skytterlag')"
    - "Splash screen logo alt text reads 'Skytterappen'"
    - "Service worker cache version prefix is 'skytterappen-' (dev: 'skytterappen-dev'; prod: 'skytterappen-<hash>')"
    - "Splash screen sessionStorage key is 'skytterappen-splash-shown'"
    - "No occurrence of 'Stordalen Skytterlag' remains under src/ or in static/manifest.json"
  artifacts:
    - path: "static/manifest.json"
      provides: "PWA install identity (name, short_name, description)"
      contains: '"name": "Skytterappen"'
    - path: "src/app.html"
      provides: "iOS PWA title meta tag"
      contains: 'content="Skytterappen"'
    - path: "vite.config.ts"
      provides: "Service worker cache version generation"
      contains: "'skytterappen-'"
    - path: "src/lib/components/Splash.svelte"
      provides: "Splash component with renamed sessionStorage key + generic alt"
      contains: "skytterappen-splash-shown"
    - path: "static/sw.js"
      provides: "Built service worker with skytterappen- cache prefix"
      contains: "skytterappen-"
  key_links:
    - from: "vite.config.ts swVersionPlugin"
      to: "static/sw.js"
      via: "writeFileSync at buildStart"
      pattern: "skytterappen-(dev|[a-f0-9]+)"
    - from: "src/lib/components/Splash.svelte"
      to: "browser sessionStorage"
      via: "getItem/setItem with key 'skytterappen-splash-shown'"
      pattern: "skytterappen-splash-shown"
---

<objective>
Replace every user-facing 'Stordalen Skytterlag' / 'Stordalen' identity string in
the PWA shell, route templates, splash component, and service worker cache prefix
with the chosen generic app name `Skytterappen`. Regenerate `static/sw.js` from
the updated Vite plugin so the committed service worker carries the new cache
prefix. Asset filenames (`stordalen.jpg`) and import identifiers
(`stordalenLogo`) are deliberately NOT changed — they are deferred to Phase 6
per RESEARCH.md.

Purpose: Satisfy BRAND-01 — the app carries no club-specific identity in code.
This unblocks Phase 6 (multi-club routing) which will replace static branding
with dynamic per-club rendering.

Output: Nine source/config files edited in-place + one regenerated `static/sw.js`.
No new files, no new packages, no new imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-generic-branding/05-RESEARCH.md
@.planning/phases/05-generic-branding/05-PATTERNS.md
@.planning/phases/05-generic-branding/05-VALIDATION.md

<interfaces>
<!-- Existing identifiers and imports that MUST NOT change (asset paths) -->
<!-- and the exact strings that MUST change. Executor uses these directly. -->

Imports that stay unchanged (asset rename is Phase 6 scope):
- `import stordalenLogo from '$lib/assets/stordalen.jpg'` in:
  - src/routes/+layout.svelte (line 3)
  - src/routes/+error.svelte (line 2)
  - src/lib/components/Splash.svelte (line 3)
- Manifest icon `src` paths `/stordalen.jpg` (both 192x192 and 512x512)
- HTML favicon hrefs `%sveltekit.assets%/stordalen.jpg` in src/app.html
- `static/sw.template.js` asset path references (NOT touched)

vite.config.ts function signature (unchanged):
- `swVersionPlugin(): Plugin` exported via default plugins array
- `buildStart()` hook writes `static/sw.js` from `static/sw.template.js`

Splash component public API (unchanged):
- `let { show = $bindable(false) } = $props()`
- Used in `src/routes/+page.svelte` as `<Splash bind:show={showSplash} />`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename PWA shell identity (manifest, iOS title, service-worker cache, splash key/alt)</name>
  <files>static/manifest.json, src/app.html, vite.config.ts, src/lib/components/Splash.svelte</files>
  <read_first>
    - static/manifest.json
    - src/app.html
    - vite.config.ts
    - src/lib/components/Splash.svelte
    - .planning/phases/05-generic-branding/05-RESEARCH.md (string inventory + Code Examples sections)
    - .planning/phases/05-generic-branding/05-PATTERNS.md (target states per file)
  </read_first>
  <action>
    Apply in-place string substitutions across four PWA shell files. Concrete target values:

    static/manifest.json (top three fields only — leave start_url, display, icons, categories, lang untouched):
    - "name": "Stordalen Skytterlag" → "name": "Skytterappen"
    - "short_name": "Stordalen" → "short_name": "Skytterappen"
    - "description": "Skyteprogram og skyttere for Stordalen Skytterlag" → "description": "Skyteprogram og resultater for skytterlaget"

    src/app.html (line 17 only — leave favicon/apple-touch-icon hrefs untouched):
    - `<meta name="apple-mobile-web-app-title" content="Stordalen" />` → `<meta name="apple-mobile-web-app-title" content="Skytterappen" />`

    vite.config.ts (inside swVersionPlugin buildStart, both literals):
    - `version = 'stordalen-dev';` → `version = 'skytterappen-dev';`
    - `version = 'stordalen-' + hash;` → `version = 'skytterappen-' + hash;`
    Do NOT touch the function name, plugin export, readFileSync/writeFileSync calls, or `static/sw.template.js` path.

    src/lib/components/Splash.svelte (two concerns, one file):
    - Line ~10: `sessionStorage.getItem('stordalen-splash-shown')` → `sessionStorage.getItem('skytterappen-splash-shown')`
    - Line ~15: `sessionStorage.setItem('stordalen-splash-shown', 'true')` → `sessionStorage.setItem('skytterappen-splash-shown', 'true')`
    - Line ~33: `alt="Stordalen Skytterlag"` → `alt="Skytterappen"`
    Do NOT change the `import stordalenLogo from '$lib/assets/stordalen.jpg'` line, the `$bindable` prop signature, or any template structure.

    Tabs (not spaces) for indentation. Single quotes. No trailing commas. Match each file's existing style.
  </action>
  <verify>
    <automated>grep -n '"name": "Skytterappen"' static/manifest.json &amp;&amp; grep -n '"short_name": "Skytterappen"' static/manifest.json &amp;&amp; grep -n 'content="Skytterappen"' src/app.html &amp;&amp; grep -n "skytterappen-dev" vite.config.ts &amp;&amp; grep -n "skytterappen-' + hash" vite.config.ts &amp;&amp; grep -n "skytterappen-splash-shown" src/lib/components/Splash.svelte &amp;&amp; grep -n 'alt="Skytterappen"' src/lib/components/Splash.svelte</automated>
  </verify>
  <acceptance_criteria>
    - static/manifest.json contains `"name": "Skytterappen"` exactly once
    - static/manifest.json contains `"short_name": "Skytterappen"` exactly once
    - static/manifest.json contains `"description": "Skyteprogram og resultater for skytterlaget"` exactly once
    - static/manifest.json still contains `"src": "/stordalen.jpg"` for both icon entries (asset filename unchanged)
    - src/app.html line containing `apple-mobile-web-app-title` has attribute `content="Skytterappen"`
    - src/app.html still references `%sveltekit.assets%/stordalen.jpg` in favicon links (asset paths unchanged)
    - vite.config.ts contains literal `'skytterappen-dev'`
    - vite.config.ts contains literal `'skytterappen-' + hash`
    - vite.config.ts contains NO occurrence of `'stordalen-dev'` or `'stordalen-' + hash`
    - src/lib/components/Splash.svelte contains `sessionStorage.getItem('skytterappen-splash-shown')`
    - src/lib/components/Splash.svelte contains `sessionStorage.setItem('skytterappen-splash-shown', 'true')`
    - src/lib/components/Splash.svelte contains `alt="Skytterappen"`
    - src/lib/components/Splash.svelte still contains `import stordalenLogo from '$lib/assets/stordalen.jpg'`
    - `yarn check` exits 0 after edits
  </acceptance_criteria>
  <done>All four PWA shell files updated to `Skytterappen` identity; asset imports and filenames preserved; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rename visible identity in route templates (titles, layout alt, error alt + heading)</name>
  <files>src/routes/+page.svelte, src/routes/skyttere/+page.svelte, src/routes/premieliste/+page.svelte, src/routes/+layout.svelte, src/routes/+error.svelte</files>
  <read_first>
    - src/routes/+page.svelte
    - src/routes/skyttere/+page.svelte
    - src/routes/premieliste/+page.svelte
    - src/routes/+layout.svelte
    - src/routes/+error.svelte
    - .planning/phases/05-generic-branding/05-PATTERNS.md (each route's target state block)
  </read_first>
  <action>
    Apply in-place edits across five route files. Title format is `<Page name> - Skytterappen` (literal, per-file — no shared constant, matches existing pattern). Concrete target strings:

    src/routes/+page.svelte (inside `<svelte:head>`):
    - `<title>Skyteplan - Stordalen Skytterlag</title>` → `<title>Skyteplan - Skytterappen</title>`

    src/routes/skyttere/+page.svelte (inside `<svelte:head>`):
    - `<title>Skyttere - Stordalen Skytterlag</title>` → `<title>Skyttere - Skytterappen</title>`

    src/routes/premieliste/+page.svelte (inside `<svelte:head>`):
    - `<title>Premieliste - Stordalen Skytterlag</title>` → `<title>Premieliste - Skytterappen</title>`
    - Leave the adjacent `<meta name="description" content="Skyttere som har oppnådd premier" />` line untouched.

    src/routes/+layout.svelte (header img inside `<a href="/">`):
    - `alt="Stordalen Skytterlag"` → `alt="Skytterappen"`
    - Do NOT change `src={stordalenLogo}`, class names, or the `import stordalenLogo` line.

    src/routes/+error.svelte (header block):
    - `<img src={stordalenLogo} alt="Stordalen Skytterlag" class="h-8 w-auto sm:h-10" />` → set alt to `"Skytterappen"`
    - `<span class="hidden text-lg font-semibold text-gray-900 sm:inline">Stordalen Skytterlag</span>` → inner text to `Skytterappen`
    - Leave the Norwegian error heading "Kunne ikke laste data. Sjekk nettforbindelsen og prøv igjen." and surrounding markup unchanged.
    - Do NOT change the `import stordalenLogo` line.

    Tabs for indentation. Single quotes in script blocks, double quotes in attributes — match each file's existing style.
  </action>
  <verify>
    <automated>grep -n '<title>Skyteplan - Skytterappen</title>' src/routes/+page.svelte &amp;&amp; grep -n '<title>Skyttere - Skytterappen</title>' src/routes/skyttere/+page.svelte &amp;&amp; grep -n '<title>Premieliste - Skytterappen</title>' src/routes/premieliste/+page.svelte &amp;&amp; grep -n 'alt="Skytterappen"' src/routes/+layout.svelte &amp;&amp; grep -n 'alt="Skytterappen"' src/routes/+error.svelte &amp;&amp; grep -n '>Skytterappen<' src/routes/+error.svelte</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/+page.svelte contains `<title>Skyteplan - Skytterappen</title>`
    - src/routes/skyttere/+page.svelte contains `<title>Skyttere - Skytterappen</title>`
    - src/routes/premieliste/+page.svelte contains `<title>Premieliste - Skytterappen</title>`
    - src/routes/premieliste/+page.svelte still contains `<meta name="description" content="Skyttere som har oppnådd premier" />`
    - src/routes/+layout.svelte header `<img>` for the logo has `alt="Skytterappen"`
    - src/routes/+error.svelte header `<img>` has `alt="Skytterappen"` AND the visible `<span class="hidden text-lg font-semibold text-gray-900 sm:inline">` contains text `Skytterappen`
    - src/routes/+layout.svelte still contains `import stordalenLogo from '$lib/assets/stordalen.jpg'`
    - src/routes/+error.svelte still contains `import stordalenLogo from '$lib/assets/stordalen.jpg'`
    - `grep -rn 'Stordalen Skytterlag' src/routes/` returns no matches
    - `yarn check` exits 0 after edits
  </acceptance_criteria>
  <done>All three page titles end in `- Skytterappen`; layout + error visual surfaces show `Skytterappen`; asset imports preserved; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate static/sw.js via yarn build, then run full BRAND-01 verification suite</name>
  <files>static/sw.js</files>
  <read_first>
    - vite.config.ts (confirm swVersionPlugin still wired into defineConfig plugins array)
    - static/sw.template.js (confirm `__CACHE_VERSION__` placeholder is intact)
    - static/sw.js (current committed file — confirm it still carries `stordalen-` prefix BEFORE build, so the regen is observable)
    - .planning/phases/05-generic-branding/05-VALIDATION.md (sampling rate + full suite command)
  </read_first>
  <action>
    Run `yarn build` from the repo root. The Vite `swVersionPlugin` buildStart hook reads `static/sw.template.js`, replaces `__CACHE_VERSION__` with `skytterappen-<git-short-sha>` (or `skytterappen-unknown`/`skytterappen-<vercel-sha-7>` if git unavailable), and writes `static/sw.js`. After the build completes, stage the regenerated `static/sw.js`.

    Then run the full BRAND-01 verification suite:
    1. `yarn check` — type-check must exit 0
    2. `grep -rn "Stordalen Skytterlag" src/ static/manifest.json` — must produce zero matches (grep exit code 1)
    3. `grep -n "stordalen-" static/sw.js` — must produce zero matches (the regenerated file must carry the new prefix)
    4. `grep -n "skytterappen-" static/sw.js` — must produce exactly one match (the cache version line)

    Do NOT manually edit `static/sw.js` — it is a generated artifact. Do NOT touch `static/sw.template.js` (asset paths in the template are Phase 6 scope per RESEARCH.md anti-patterns).
  </action>
  <verify>
    <automated>yarn build &amp;&amp; yarn check &amp;&amp; ! grep -rn "Stordalen Skytterlag" src/ static/manifest.json &amp;&amp; ! grep -n "stordalen-" static/sw.js &amp;&amp; grep -c "skytterappen-" static/sw.js</automated>
  </verify>
  <acceptance_criteria>
    - `yarn build` exits 0
    - `yarn check` exits 0
    - `grep -rn "Stordalen Skytterlag" src/ static/manifest.json` returns no matches (exit code 1)
    - `static/sw.js` contains at least one occurrence of `skytterappen-`
    - `static/sw.js` contains no occurrence of `stordalen-`
    - `static/sw.template.js` is unchanged (still contains `__CACHE_VERSION__` placeholder and original asset references)
    - `static/sw.js` is staged as a modified tracked file (regenerated by build)
  </acceptance_criteria>
  <done>Build succeeds, type-check clean, regenerated `static/sw.js` carries the `skytterappen-` cache prefix, and grep proves zero remaining `Stordalen Skytterlag` strings in src/ or manifest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none for this phase) | Phase 5 is string-replacement + docs only; no auth, input validation, crypto, or session management changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | N/A | All Phase 5 surfaces | accept | Per security_context: Phase 5 has no security surface. No ASVS categories apply. No new packages installed, no new external inputs, no new persisted state beyond a renamed sessionStorage key (non-security-sensitive UI flag). |
</threat_model>

<verification>
After all three tasks complete:

1. `yarn build && yarn check` — both exit 0
2. `grep -rn "Stordalen Skytterlag" src/ static/manifest.json` — zero matches
3. `grep -n "skytterappen-" static/sw.js` — at least one match
4. `grep -n "stordalen-" static/sw.js` — zero matches
5. Manual smoke (optional, deferred to phase-level UAT): open `yarn dev` locally; the browser tab title on `/` reads `Skyteplan - Skytterappen`; on `/skyttere` reads `Skyttere - Skytterappen`; on `/premieliste` reads `Premieliste - Skytterappen`; splash screen logo carries alt `Skytterappen` when sessionStorage is cleared.
</verification>

<success_criteria>
- All 9 source/config files updated to use `Skytterappen` identity (manifest.json, app.html, vite.config.ts, Splash.svelte, +page.svelte, skyttere/+page.svelte, premieliste/+page.svelte, +layout.svelte, +error.svelte)
- Regenerated `static/sw.js` carries `skytterappen-` cache prefix
- Asset imports (`stordalenLogo` from `$lib/assets/stordalen.jpg`) and asset paths (`/stordalen.jpg` in manifest icons + app.html favicons) preserved unchanged — Phase 6 scope
- `package.json` `name` field untouched — internal npm name, not user-facing
- `yarn check` and `yarn build` both exit 0
- Zero occurrences of `Stordalen Skytterlag` in `src/` or `static/manifest.json`
- BRAND-01 requirement fully covered (manifest, browser tab title, README is covered separately by Plan 02)
</success_criteria>

<output>
Create `.planning/phases/05-generic-branding/05-01-SUMMARY.md` when done, documenting:
- Files modified (with one-line description of each change)
- New cache prefix observed in regenerated `static/sw.js`
- Confirmation that asset imports/paths are preserved for Phase 6
- Any deviations from plan (none expected)
</output>
