---
phase: 05-generic-branding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - BRAND-02
tags:
  - docs
  - deployment
  - dns

must_haves:
  truths:
    - "README.md describes this project (Skytterappen PWA), not the default SvelteKit scaffold"
    - "README.md contains a '## Deployment Prerequisites' section"
    - "Deployment Prerequisites section names the chosen domain (skytterappen.no or skytterappen.app, marked TBD pending owner confirmation)"
    - "Deployment Prerequisites section states wildcard DNS requirement with the exact pattern `*.<domain> CNAME cname.vercel-dns.com`"
    - "Deployment Prerequisites section states Vercel wildcard domain configuration requirement"
    - "Deployment Prerequisites section notes clubs.ts subdomain keys must match the DNS pattern (forward reference to Phase 6)"
    - "README.md contains no occurrence of 'Stordalen Skytterlag' as user-facing branding"
  artifacts:
    - path: "README.md"
      provides: "Project README naming app as 'Skytterappen' + deployment prerequisites for Phase 6"
      contains: "Skytterappen"
      min_lines: 25
  key_links:
    - from: "README.md Deployment Prerequisites section"
      to: "Phase 6 multi-club routing"
      via: "forward reference: wildcard DNS + Vercel wildcard domain are prerequisites before Phase 6 goes live"
      pattern: "wildcard"
---

<objective>
Replace the default SvelteKit scaffold README (`# sv` / "Everything you need to
build a Svelte project") with a project-specific README that names the app
generically as `Skytterappen` and documents the manual deployment prerequisites
that block Phase 6 from going live (domain registration + wildcard DNS + Vercel
wildcard domain configuration).

Purpose: Satisfy BRAND-02 — domain registration is a manual ops step outside
code, but the requirement must be documented so the next operator (the owner,
or a future Claude session) knows exactly what to configure before Phase 6
ships. STATE.md blockers section explicitly calls this out.

Output: One file written from scratch (README.md). No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-generic-branding/05-RESEARCH.md
@.planning/phases/05-generic-branding/05-PATTERNS.md

<interfaces>
<!-- Source content for the README. All values verified from existing planning artifacts. -->

Project identity (from RESEARCH.md + ROADMAP.md):
- App name: Skytterappen
- Core value: "Shooters can instantly see today's schedule and their results on their phone at the range — no friction, no loading confusion."
- Stack (from CLAUDE.md): SvelteKit 2.x, Svelte 5.x, TypeScript 5.x, TailwindCSS 4.x, Vite 7.x, Yarn package manager, Node 22.x
- Data source: leonls.kongsberg-ts.no GraphQL API via graphql-request
- Deployment target: Vercel (@sveltejs/adapter-vercel)

Domain candidates (from RESEARCH.md — owner choice, mark TBD):
- skytterappen.no
- skytterappen.app

Wildcard DNS pattern (from RESEARCH.md BRAND-02 section):
- DNS record: `*.<domain> CNAME cname.vercel-dns.com`
- Vercel: add `*.<domain>` as wildcard domain on project
- Effect: any subdomain (e.g. `stordalen.<domain>`, `myclub.<domain>`) resolves to the same Vercel deployment

Phase 6 forward reference (from ROADMAP.md):
- Phase 6 implements `src/lib/clubs.ts` mapping subdomain → club config
- Phase 6 resolves club from HTTP host header server-side
- Phase 6 retires the `?c=` query param

Existing scripts (from package.json conventions in CLAUDE.md):
- `yarn dev` — dev server
- `yarn build` — production build
- `yarn check` — type-check
- `yarn lint` / `yarn format` — lint + prettier
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write project-specific README replacing SvelteKit scaffold, including Deployment Prerequisites section</name>
  <files>README.md</files>
  <read_first>
    - README.md (current default SvelteKit scaffold — confirm baseline before overwrite)
    - .planning/phases/05-generic-branding/05-RESEARCH.md (BRAND-02 documentation section + app name rationale)
    - .planning/REQUIREMENTS.md (BRAND-02 exact wording)
    - .planning/ROADMAP.md (Phase 6 description for forward reference accuracy)
    - CLAUDE.md (stack details: Node 22.x, Yarn, Vercel adapter)
  </read_first>
  <action>
    Overwrite README.md (currently the default `# sv` SvelteKit scaffold) with a project-specific README. The new file MUST include the following sections in this order, using these exact concrete values:

    1. Top-level heading: `# Skytterappen`
    2. One-sentence tagline naming this as a PWA for Norwegian rifle shooting clubs serving today's schedule, shooter results, and prize lists at the range.
    3. `## Stack` — bulleted list including: SvelteKit 2.x, Svelte 5.x (runes), TypeScript 5.x, TailwindCSS 4.x, Vite 7.x, graphql-request 7.x, Node 22.x, Yarn, deployed on Vercel via `@sveltejs/adapter-vercel`.
    4. `## Data Source` — one paragraph naming the external GraphQL API (leonls.kongsberg-ts.no) and noting it is read-only and external (cannot change API shape).
    5. `## Development` — list the commands `yarn install`, `yarn dev`, `yarn build`, `yarn check`, `yarn lint`, `yarn format`.
    6. `## Deployment Prerequisites` — MANDATORY for BRAND-02. Must contain ALL of:
       - A line stating the chosen domain is `skytterappen.no` or `skytterappen.app`, marked **TBD (owner to confirm and register)**.
       - A subsection or paragraph stating wildcard DNS must be configured before Phase 6 goes live, with the exact DNS record shown as a code block: `*.<domain> CNAME cname.vercel-dns.com`
       - A line stating that `*.<domain>` must be added as a wildcard domain on the Vercel project (Vercel dashboard → project → Domains → Add → wildcard).
       - A forward reference to Phase 6: "Phase 6 introduces `src/lib/clubs.ts` mapping subdomain → club configuration. The subdomain keys in `clubs.ts` must match the DNS pattern (e.g. `stordalen` subdomain key requires `stordalen.<domain>` DNS to resolve)."
       - A note that domain registration and DNS configuration are MANUAL human steps outside code — Phase 5 only documents the requirement; Phase 6 ships the routing code.
    7. `## Project Structure` — brief bulleted summary referencing the three routes (`/` schedule, `/skyttere` shooters, `/premieliste` premieliste) and key directories (`src/lib/graphql/`, `src/lib/components/`, `src/routes/`). One sentence per item; do not duplicate CLAUDE.md.

    Format rules:
    - Use tabs for any code blocks if they contain indented code (per repo style).
    - Use fenced code blocks for shell commands and DNS records.
    - Do NOT include the string `Stordalen Skytterlag` anywhere — this README is generic.
    - Do NOT mention `?c=` query param as a feature — it is being retired in Phase 6 (per ROADMAP).
    - Length: ≥ 25 lines (substantive, not boilerplate). Target ~60–100 lines.
    - Norwegian text is allowed where natural (the app's UI is Norwegian); section headings should remain English to match the existing project documentation pattern in `.planning/`.
  </action>
  <verify>
    <automated>grep -n '^# Skytterappen' README.md &amp;&amp; grep -n '^## Deployment Prerequisites' README.md &amp;&amp; grep -n 'cname.vercel-dns.com' README.md &amp;&amp; grep -n 'wildcard' README.md &amp;&amp; grep -n 'clubs.ts' README.md &amp;&amp; grep -n 'TBD' README.md &amp;&amp; ! grep -n 'Stordalen Skytterlag' README.md &amp;&amp; ! grep -n '^# sv$' README.md &amp;&amp; [ "$(wc -l < README.md)" -ge 25 ]</automated>
  </verify>
  <acceptance_criteria>
    - README.md first line is `# Skytterappen`
    - README.md contains a heading `## Deployment Prerequisites`
    - README.md contains the literal string `cname.vercel-dns.com`
    - README.md contains the literal string `*.` (wildcard DNS pattern indicator) in a DNS-context line or code block
    - README.md contains the word `wildcard` (referring to DNS / Vercel domain config)
    - README.md contains the literal string `clubs.ts` (Phase 6 forward reference)
    - README.md contains the literal string `TBD` (marking domain choice as owner decision)
    - README.md contains no occurrence of `Stordalen Skytterlag`
    - README.md does NOT contain the original scaffold heading `# sv`
    - README.md is at least 25 lines long
    - README.md mentions `Vercel` (deployment target)
    - README.md mentions `SvelteKit` (framework)
    - README.md mentions all four operational yarn scripts: `yarn dev`, `yarn build`, `yarn check`, `yarn install`
    - README.md mentions Phase 6 by name OR by capability (multi-club routing / host-header resolution) — proving the forward reference exists
  </acceptance_criteria>
  <done>README.md describes Skytterappen, lists its stack and scripts, and documents domain + wildcard DNS + Vercel + clubs.ts prerequisites for Phase 6. Default SvelteKit scaffold content removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none for this phase) | Documentation-only change; no code, no inputs, no runtime surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | N/A | README.md | accept | Per security_context: Phase 5 has no security surface. README is static documentation; no auth, input handling, crypto, or session management involved. Documenting deployment prerequisites does not introduce a threat — it reduces operational risk by surfacing manual DNS requirements. |
</threat_model>

<verification>
After the task completes:

1. `head -1 README.md` — output is exactly `# Skytterappen`
2. `grep -c "Skytterappen" README.md` — at least 2 matches (title + body)
3. `grep -n "cname.vercel-dns.com" README.md` — exactly one match (DNS record line)
4. `wc -l README.md` — ≥ 25 lines
5. Manual review: read README.md top-to-bottom; confirm Deployment Prerequisites section is unambiguous and a non-Claude operator could execute the DNS + Vercel steps from it.
</verification>

<success_criteria>
- README.md replaced (no SvelteKit scaffold content remaining)
- BRAND-02 requirement fully covered: chosen domain documented (with TBD marker), wildcard DNS pattern shown verbatim, Vercel wildcard domain step listed, clubs.ts forward reference present, manual-step nature called out
- No occurrence of `Stordalen Skytterlag` in README.md (BRAND-01 alignment — README is now generic)
- File is substantive (≥ 25 lines, target 60–100 lines), not stub
- All acceptance_criteria grep assertions pass
</success_criteria>

<output>
Create `.planning/phases/05-generic-branding/05-02-SUMMARY.md` when done, documenting:
- The chosen app name as written (`Skytterappen`)
- The domain candidates listed (and TBD status)
- The exact DNS record string committed to README
- Confirmation that Phase 6 forward reference (clubs.ts) is present
</output>
